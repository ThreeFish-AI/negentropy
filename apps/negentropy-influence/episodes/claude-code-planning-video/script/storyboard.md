# 分镜：《AI 的视野是安排出来的：写下来的计划，另开的桌子》

> 逐字稿 SSOT：[narration.md](./narration.md)（句 id 即本表的定位锚）；视觉契约见 [planning.md](./planning.md) §三。
> 一镜（beat）= 2–8 句连续句子共享同一主画面；句区间必须**覆盖本幕每一句**（`check_script.py` 强制）。
> 色名对应 [../video/src/design/theme.ts](../video/src/design/theme.ts)：`core` 陶土橙（系列锚环，桌后）·
> `view` 鸢紫（本集己色：被安排的视野）· `mech` 石青（管理视野的机制）· `deny` 警示红。
> **镜号必须与 `scenes/*.tsx` 里 `<Sequence name>` 逐字一致**（抽帧 QA 对照用）。

## P0 它没有「昨天」

| 镜 | 句区间 | 画面 | 动效 |
|---|---|---|---|
| 0-A | p0-01..03 | 系列同款终端：任务单首行被工具输出顶出可视区、走秒芯片跳动；第三句出口时右下角闪一枚「收回」钢印红色虚影，停半拍淡出 | 打字机推进 + 首行任务单向上滚出（残影停在顶端变 `dim`）；p0-03「收回」钢印砸下（`deny` 色虚影、scale 过冲）后淡出——退位悬念第一分钟埋下 |
| 0-B | p0-04..05 | 终端缩小上移，下方铺开「桌面」：全部对话内容平铺成色块行，任务单色块被越推越远 | 桌面色块逐块落下堆高；任务单色块（`core` 描边）持续被推向右端、被挤出桌沿淡出；p0-05 主线问句两行 `serif` 浮入 |
| 0-C | p0-06..09 | 桌面定格；环（`core`）在桌后浮现（半透明）；答句「不是——有一整套装置」压屏；五样装置图标列队；**四件套标尺**（p0-08/09：循环／工具／上下文管理／护栏四格横排，前两格与末格 `panelBorder` 虚线 `dim` 字不点亮，「上下文管理」格以 `view` 描边点亮但只填左半格，格下小字「今天补前半」） | 环描线一圈但保持 35% 透明度（「今天讲桌子，环在后面看着」）；五样装置小图标自左向右依次点亮（清单/副桌/目录/垫纸/梯子）；p0-08 起标尺自桌下升起、四格逐格描边；p0-09 起「上下文管理」格左半以 `viewDeep` 线性填充（24 帧），格下小字浮现 |

## P1 把清单钉在桌上

| 镜 | 句区间 | 画面 | 动效 |
|---|---|---|---|
| 1-A | p1-01..03 | 桌面主舞台。右侧滑入第一样装置：一张三态清单卡（pending/in_progress/completed 三行，`view` 描边） | 清单卡滑入落桌；三行状态图标依次闪现（空格→▸→✓ 各一次演示态） |
| 1-B | p1-05..09 | 清单卡「啪」地钉进对话流：桌面上的色块流里插入一张始终可见的 `view` 卡；工具输出继续堆积但推不动它 | 图钉落下（spring 过冲）+ 卡固定在桌面左上；后续色块撞上卡被「顶开」滑落——「钉住」的物理感 |
| 1-C | p1-10..13 | 左下角小代码卡：注册表两行 diff（TOOLS+1 / TOOL_HANDLERS+1，`mech` 高亮）；右侧金句卡 | 两行 diff 逐行高亮；p1-12 金句卡 `serif` 淡入「增加的不是执行能力，是规划能力」 |
| 1-D | p1-14..15 | **退位帧**：官方公告条压入——新一代模型上清单工具默认不再安装；「收回」钢印（`motifs.Stamp`，`deny`，damping:12 过冲 + ripple）盖在清单卡上；「押注押到期」标签 | 官方公告条淡入（code.claude.com 口径角标）；钢印砸下带过冲与涟漪，容器同帧 `translateY(3px)` 下沉、8 帧回弹；被盖章的 `todo_write` 卡本体有反应：描边由 `view` 褪为 `panelBorder`、三行状态字依次划线、整卡下沉 12px 并降到 0.45 透明 |
| 1-E | p1-15a..15e | **新镜「计划闸」**：只读探索的手伸向文件，被一层玻璃罩罩住（`mech` 描边 + 8% 白底）；一张 `view` 描边的计划卡从上落桌；「改文件」动作块撞上玻璃罩被弹回；三选一按钮横排（自动闸门／逐条点头／继续再想，反枚举：`panel` 底 + 编号 01/02/03，仅当前项染 `mech`）；末句金句卡 | p1-15a 玻璃罩自上罩下（spring）；p1-15b 计划卡落桌（damping:200）；p1-15c 动作块撞罩 `deny` 弹回（复用 3-C 单向阀 bounce 曲线）；p1-15d 三按钮逐个描边亮起；p1-15e 切 `QuoteCard`「清单写给它，计划写给你」 |
| 1-F | p1-16..19 | 任务系统预告一句（带依赖/能锁/落盘小图标）+「装在循环外的能力」收束；**零件生命周期时间轴**：左端「模型记不住 → 需要清单」、右端「模型记住了 → 清单过期」 | 任务系统小图标一闪（「以后单拆」）；时间轴描出，一枚 `core` 光点自左滑向右，抵达右端时清单卡彻底消失；p1-18/19 金句短卡 |

## P2 另开一张副桌

| 镜 | 句区间 | 画面 | 动效 |
|---|---|---|---|
| 2-A | p2-01..04 | 桌面色块暴涨成灾：一百多条记录把桌面填满，任务相关的高亮块被淹没 | 色块快速堆叠到溢出桌沿；计费计数器（`mech`）持续跳字——「一直占着位置、一直计着费」 |
| 2-B | p2-05..07 | 桌面右侧滑出一张小副桌（`mech` 描边，自带一枚缩小克隆环），主桌上「翻找」的脏纸堆整体飞向副桌 | 副桌滑入（spring）；纸堆打包飞行；副桌上展开全新的三行对话（干净态：仅一句任务说明） |
| 2-C | p2-08..11 | 分屏：副桌上分身自己跑循环（小环转动 + 工具调用闪现）；主桌只收到一张回执卡（`view`） | 副桌小环匀速转、色块在副桌堆叠不影响主桌；干完一张回执卡飞回主桌落定，副桌纸堆淡出 |
| 2-D | p2-12..15 | 两条纪律：副桌工具卡阵列中「派活」卡为 `deny` 描边+锁定图标；分身动手前迷你闸门（P3 语言复用）逐次落下 | 「派活」卡打叉上锁；小闸门落下、请求过闸打点——「隔离的是视野，不是权限」 |
| 2-E | p2-16..24 | ★深挖帧：两桌并置，中间一排「等号锁」——五要素卡（系统提示/工具表/模型/消息前缀/思考配置）逐张比对，全部对上后锁扣「咔」合拢、缓存印章（SAVE）砸下 | 五要素卡左右滑入逐字节对齐（每对上一张亮 `mech`）；全对齐后等号锁合拢 + 印章落下的冲击波；角标「fork 继承完整对话 · 共享提示缓存 —— 官方文档 sub-agents」 |
| 2-F | p2-25..30 | 半拉隔离：副桌下方一个「读过的文件」抽屉在两桌间**共享**（虚线双桌共连）；弹窗冒泡：副桌的审批卡沿虚线上浮到主桌屏幕；**三只笼子**（p2-28a..c：同时在场 ≤20 / 轮数上限 / 上下文配额烧完收工）；末句收束卡 | 抽屉盖半开、两端各画一只手同时拉开；审批卡气泡沿路径上浮；三只笼子卡依次落下；p2-29 收束句「脏活不占主桌」金句卡 |

## P3 目录卡与垫纸

| 镜 | 句区间 | 画面 | 动效 |
|---|---|---|---|
| 3-A | p3-01..03 | 左：桌角一叠薄目录卡（每张一行，标价 ~100）；右对照：整块厚手册拍上桌（标价 ~2000+），桌面被压得下沉 | 厚手册落下压桌（下坠 8px 抖动）；计价器快速跳字到红色——「99% 无关」的重量感 |
| 3-B | p3-04..07 | 两级加载：目录卡摊开成一排（各标一行名+用途）；一张卡被抽中 → 对应厚手册从书架拉开上桌 | 目录卡扇形展开；抽中卡 `view` 高亮；手册拉开时计价器从 100 跳到 2000（`mech` 数字滚动） |
| 3-C | p3-08..10 | 预算硬顶：目录排上方一条 `mech` 预算横杆（~1%/上限标注）；取手册走「注册表名→内容」的单向阀，不走文件路径 | 横杆落下限位；路径请求（`../` 字样卡）撞上单向阀被 `deny` 弹回——防目录穿越 |
| 3-D | p3-11..15 | 垫纸（system prompt）登场：桌面最大的一张 `view` 纸，从「一整块写死」碎成四段卡（身份/工具/工作目录/相关记忆） | 写死整纸先满屏铺开（`dim`）；碎裂动画拆成四段卡；每轮循环四段卡重新拼装一次（滑入对齐） |
| 3-E | p3-16..19 | 「真实状态」验证：左侧「查注册表/查文件存在」两个探针动作；右侧被否决的「猜关键词」路径（消息里高亮词 + 问号）打叉 | 探针（`mech` 小手）点向分发表与文件图标，各亮一次；关键词路径整条 `deny` 划掉；拼装结果盖「cache hit」章 |
| 3-F | p3-20..26 | ★深挖帧：先是**官方削层**——整块垫纸剖面从满高削掉八成（p3-21），旁边评测计分板数字纹丝不动并打勾，压字「删掉八成以上，评测不掉分 · 官方 2026-07」；随后静态/动态段落分层图：所有段落卡排成两层进缓存仓，唯独「外接工具段」卡被闸在仓外（`deny` 拦杆 + 掉线抖动） | p3-20 体量角标浮现（三级归属：源码分析）；p3-21 剖面 height 由 100% 插值到 20%（24 帧），削掉的部分向上碎裂淡出，计分板打勾；p3-21a 金句短卡；p3-22 起段落卡依次入仓盖章，外接段卡靠近仓门被拦杆挡住、卡身闪断（模拟掉线）；p3-25 收束句浮现 |

## P4 桌边的补救梯

| 镜 | 句区间 | 画面 | 动效 |
|---|---|---|---|
| 4-A | p4-01..02 | 桌子右缘挂下一张三级木梯（`mech` 骨架），梯级暂空 | 梯子自上垂下挂稳（rope 微弹） |
| 4-B | p4-03..07 | 一级梯「话没说完」：输出文本条中途断截；额度标尺 8K→64K 弹簧拉长一次；续写卡贴上断口，卡上写「别道歉，别复述，接着说」（角标注明为源码提示原文意译） | 文本条断口处闪 `deny`；标尺拉伸回弹原请求重放；三次续写计数点逐个亮，亮满即封条 |
| 4-C | p4-08..10 | 二级梯「桌上太满」：整桌色块压缩成一张摘要卡，桌面清空大半；第二张摘要卡试图再压时被 `deny` 拦停 | 色块群被吸成一张卡（收缩动画）；第二次压缩箭头刚起就被拦杆挡下、桌上浮「退出」 |
| 4-D | p4-11..17 | 三级梯「门外施工」：桌外一枚过载/限流警示牌落下；等待条从 0.5s 起翻倍生长（0.5→1→2→4→…→32s 封顶横杠），条身带抖动毛边；一张「稍后再来」字条贴上并改写等待值；三连 529 后模型名牌翻面换人 | 警示牌砸落抖动（p4-11）；等待条按节拍翻倍伸长、撞上 32s 封顶杠「当」一声定格（p4-12）；抖动毛边随机化一次（p4-13）；字条飘落覆盖公式值（p4-14）；名牌翻转露出备用模型（p4-17）；角标「恢复路径实测 17 条 · 第三方的源码分析」 |
| 4-D2 | p4-17a..17f | **新镜「不重试的两类」**：梯子右侧岔出一条 `deny` 色分叉轨，轨口牌子「不重试」；一枚证书图标滑上轨、第一帧就打红叉并弹「去修它」；屏幕中央一条输出文本条断在半途 + 一枚已发出的工具调用卡；★形变帧：工具卡**原地分裂成两张**各跑一遍同样的动作、右下角计数器 1→2，压字「同一个动作跑了两遍」；两卡收回成一张并贴「没写完」标签，`core` 光点从上一个已完成调用处续向右；右下角刻度盘 0→10、外圈虚线刻到 15 | p4-17a 分叉轨 `pathLength` 描线 26 帧；p4-17b 证书卡滑入即打叉；p4-17c 文本条断口 ▌ 闪烁 + 工具卡同帧闪 `deny`；p4-17d 工具卡 scaleX 1→2 中缝裂开成两张、计数器滚动；p4-17e 两卡合一 + 标签贴上 + 光点续接；p4-17f 刻度盘 `Counter` 0→10（20 帧），外圈 dashed 描边 +「上限 15」小字 |
| 4-E | p4-19..22 | 收益递减：产出曲线三次爬升依次趋平，第三次的增量 < 阈值标线，系统盖「停」章（角标「第三方的源码分析」）；**检查点卡**（p4-21a/b：改动回退到检查点，只含文件改动——命令与外部状态不在其中）；末句金句卡 | 曲线逐段绘制、斜率递减；标线横贯；「停」章（`deny`）落下；检查点卡自下滑入；p4-22 金句「每一级补救，都带着上限」淡入 |

## P5 桌子换了五样，环没动

| 镜 | 句区间 | 画面 | 动效 |
|---|---|---|---|
| 5-A | p5-01..06 | 五样装置在桌面上归位成一组（清单卡/副桌缩略/目录卡/垫纸/梯子缩略），各配一字职责（别忘/别堵/别贪/别写死/别断） | 五装置自左向右滑入归位；各配的职责字以印章形式逐枚落下 |
| 5-B | p5-07..09 | ★系列锚帧：环从桌后浮到画面中央（第二次出场，同色同宽同节点），桌子与五装置整体淡为线稿退后；五条外挂虚线把五样装置逐条挂到环外 | p5-07 环起浮（riseAt）；p5-08 环描线一遍 + 亮度升至 100%、桌子线稿化（描边保留填充淡出）；p5-09 五条挂钩虚线按 `strokeDashoffset` 逐条描出（stagger i×6 帧）；角标「五段增量全部为外挂 · while 骨架未换（实测 @ 67a9126c）」 |
| 5-C | p5-13..16 | 金句帧：环居中，桌上线稿，两行落点字 | 「不是它变聪明了 / 是有人替它把视野管了起来」逐行淡入（`serif`，`core`） |

## P6 落点与信源

> **改序说明（2026-08-28）**：口播已按「免责两句 → 金句 → 预告」重排——p6-07/p6-08 物理前置于 p6-01（句 id 不重编，TTS 缓存键不涉序）。镜号随之重排：6-A=信源前置、6-B=金句、6-C=收束+身份卡+进度条+渐黑。**video/src P6Ending.tsx 的 Sequence 拆分（bA0=w('p6-07','p6-08') 等）与渐黑窗口重推导由视觉侧同步落地。**

| 镜 | 句区间 | 画面 | 动效 |
|---|---|---|---|
| 6-A | p6-07..08 | **信源前置**：信源卡两行（官方文档+取数日期 / 第三方源码分析已逐处标注）压屏 | 信源卡逐行浮现（前置到幕首，免责只走画面不占情绪高点） |
| 6-B | p6-01..06 | 一句话合同金句卡（`serif`，`view` 描边）→ 三行人味延伸小字 →「清单退位/赌注过期/自省句」「桌子会换循环不停」次行 | 金句卡淡入；p6-02/03 人味延伸两行逐行浮现；p6-04/05 赌注到期+官方自省句短卡；p6-06「循环没停过」一行收进金句卡底部 |
| 6-C | p6-09 | **五格层进度条**（执行/规划/记忆/时机/协作横排：已播层 `dim` 常亮、当前层 `core` 高亮描边+底部指示条、未播层 `panel` 底+编号，反枚举不用五色）；上方压 0-C 四件套标尺定格缩略（「上下文管理」格保持半亮）；下期文案一行挂进度条下方 → **渐黑** | 进度条自左滑入、当前格描边点亮；标尺缩略淡入（一帧完成「轮内四件套 / 轮外五层」映射）；下期行打字机两拍；末 1.2s 线性压暗到全黑（窗口从本 beat 总时长推导，红线四） |
