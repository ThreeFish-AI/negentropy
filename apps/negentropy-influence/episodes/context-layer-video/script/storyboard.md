# 分镜：《自己动手，给 AI 搭一个上下文层》

> 逐字稿 SSOT：[narration.md](./narration.md)（句 id 即本表的定位锚）；视觉契约见 [planning.md](./planning.md) §三。
> 一镜（beat）= 2–8 句连续句子共享同一主画面；句区间必须**覆盖本幕每一句**（`check_script.py` 强制）。
> 色名对应 [../video/src/design/theme.ts](../video/src/design/theme.ts)：`blueprint` 工程蓝（架构/五层）·
> `grown` 苔绿（生命周期/自纠）· `activate` 暖橙（resolve/MCP 插头）· `danger` 警示红（冲突/越权/错数）。
> **镜号必须与 `scenes/*.tsx` 里内嵌 `<Sequence name="N-X">` 逐字一致**（抽帧 QA 对照用）。
> 动效列 `@动词` 对应 motion 模型（hooks.ts），`--check-motion` 机检。

## P0 你的 AI 也在裸奔吗（p0-01..09）

| 镜 | 句区间 | 画面 | 动效 |
|---|---|---|---|
| 0-A | p0-01..05 | 桌面一个 AI 助手图标，头顶三张对话卡依次弹出：周报✓（绿）/ 查活跃用户?（灰）/ 错误数字✗（红）；p0-04 一张「入职第一天」日历卡在 AI 头顶碎成粉末 | 三卡 stagger 弹出（状态色即时）；日历卡碎裂粒子四散（impulse + 粒子 travel）；AI 图标冒问号 breathe ；`@stagger` `@impulse` `@breathe` |
| 0-B | p0-06..09 | 远景一栋亮灯大楼（Snowflake 字样小角标），镜头拉近又拉回桌面；桌面上铺开一张 blueprint 蓝图纸，片名《自己动手，给 AI 搭一个上下文层》落线 | 大楼淡入→缩退（pushIn 反向）；蓝图纸四角展开（spring）；片名 serif 描线落位（draw） ；`@pushIn` `@spring` `@draw` |

## P1 五块积木（p1-01..26）

| 镜 | 句区间 | 画面 | 动效 |
|---|---|---|---|
| 1-A | p1-01..02 | 蓝图纸上打出两个大问号：「放什么？」「怎么找？」 | 问号 rise 落位 + breathe（本幕极短） ；`@enter:rise` `@breathe` |
| 1-B | p1-03..11 | **积木一/二**：第一块「对象」积木（小抽屉柜图标）落位；第二块「目录」积木——一张账本页，每行一个指针箭头指向远处的源对象；旁一条「抄写」叉路被红线划掉；p1-11 账本页边缘浮现四类信号小标签（是什么/跑得如何/含义/热度） | 两块积木先后 spring 落位（间隔 12 帧）；指针箭头 draw 逐一描出；抄写叉路划红（shake）；四标签 stagger 贴边 ；`@spring` `@draw` `@shake` `@stagger` |
| 1-C | p1-12..17 | **积木三/四/五**：富化（浇灌壶）/ 治理（盾牌）/ 激活（插口）三块积木依次落位，五块拼成一台完整机器的轮廓 | 三块积木 stagger 弹入；拼合时机身轮廓描线一圈（draw）点亮 blueprint 蓝 ；`@stagger` `@draw` |
| 1-D | p1-18..21 | **正交演示**：音响系统示意（音箱/功放/装修三层），换掉音箱层高亮而其余两层纹丝不动；旁边五积木同构映射 | 音响三层 stagger 显现；抽出音箱层（travel）→ 其余两层标「不动」徽章（impulse 点亮）；映射线 draw 连到五积木 ；`@stagger` `@travel` `@draw` |
| 1-E | p1-22..26 | **数据的一生**：一个含义光点从对象层出发，依次流经目录→富化→治理→激活五站传送带，最后落入 AI 手心 | 光点沿五站等速巡游（travel 循环一次）；每到一站该站 impulse 点亮；终点 AI 手心合拢（spring） ；`@travel` `@impulse` `@spring` |

## P2 万物皆对象（p2-01..32）

| 镜 | 句区间 | 画面 | 动效 |
|---|---|---|---|
| 2-A | p2-01..05 | 一张维基条目卡居中（标准身材：字段槽位空着）；p2-05 三个字段槽位被框出放大预告 | 条目卡 rise；字段槽 stagger 显现（6ms/槽）；三个关键槽位描金框（draw）+ impulse ；`@enter:rise` `@stagger` `@draw` |
| 2-B | p2-06..10 | **同义词**：中央「毛收入」条目；三个提问气泡（营收/进账/销售额）飞向检索窗；前两个带别名线的命中亮起，第三个「进账」无别名线撞在玻璃上（隐形条目） | 气泡 travel 飞向窗口；命中线 draw 连通 + 条目亮；「进账」气泡撞玻璃弹回（spring 反向 + shake）＋玻璃上裂纹角标 ；`@travel` `@draw` `@shake` |
| 2-C | p2-11..14 | **AI 说明书**：分屏对照——左定义卡内嵌「注意事项」行随版本 v3→v4 同步翻新（绿流）；右三张散落提示词贴纸各自蒙灰过期 | 左卡版本号 flip，说明行同步重描（draw）；右贴纸蒙灰加深（dim）；中缝 vs 对撞线 ；`@draw` `@dim` |
| 2-D | p2-15..17 | **签名问答**：问答卡 + 右下钢笔签名划线（verified_by/at）；AI 图标命中时答案行直接高亮滑出 | 签名曲线 draw 描出；命中时答案行 engine 辉光 + 署名角标 spring 弹出 ；`@draw` `@spring` |
| 2-E | p2-18..22 | **词条的一生·前半**：状态机轨道第一段——draft 起点站（灰）→ 结构校验门（三道检查项依次打勾：表存在/指向唯一键/名字唯一）→ 评审人图标盖章 → governed 站台（grown 绿亮） | 轨道描线（draw）；三检查项 stagger 打勾；校验失败支路一闪（danger 短脉冲，被拒分支演示）；盖章 impulse；站台亮起 spring ；`@draw` `@stagger` `@impulse` `@spring` |
| 2-F | p2-23..28 | **词条的一生·岔道**：governed 站台分出 conflict 岔道（两卡对峙 + 裁决灯）与 superseded 缓行线（旧卡标「已被取代」仍可被搜到、排序靠后），rejected 终点站（灰暗） | 岔道描线分叉（draw）；对峙卡 travel 相向；裁决灯落下胜卡回站台、败卡滑向终点（travel + dim）；旧卡 superseded 标签贴上（spring） ；`@draw` `@travel` `@dim` |
| 2-G | p2-29..32 | 状态机全景定格：整条轨道一次全亮，金句卡「谁说了算，从人情问题变成流程问题」（serif） | 全轨道节点 stagger 依序点亮（快节奏）；金句卡淡入压轴（progress） ；`@stagger` `@progress` |

## P3 养上下文：双轨与冲突（p3-01..29）

| 镜 | 句区间 | 画面 | 动效 |
|---|---|---|---|
| 3-A | p3-01..05 | **写不动**：一个人形图标对着巨墙打字，墙只被金漆刷出极小一角；角标「9,685 表 / <5%（Snowflake 内部实测）」；右侧新表格图标持续涌入 | 金漆填充缓慢（progress 慢档）；新表图标 flowDash 持续涌入；百分比数字 count 爬到 5% 就停 ；`@progress` `@flowDash` `@count` |
| 3-B | p3-06..09 | **双轨**：左 manual 轨「人写金标准」（权威条满格）+ 机器生成草稿经人审转正（小图标流水）；右 dig 轨「使用痕迹挖掘」（查询日志粒子汇成定义卡，权威条半格）；两轨汇入目录 | 双轨并行描线（draw）；权威条对比填充（count 两速）；汇入点 glow（impulse） ；`@draw` `@count` `@impulse` |
| 3-C | p3-10..14 | **纠错环**：考卷卡（金标准问答）→ 挖出口径作答红叉 → 自动修复（补同义词 + 调热度条）→ 重排再答绿勾；角标「本仓原型实测输出 · C4」 | 考笔迹 draw 快描；红叉 impulse；修复两动作卡 stagger；重考绿勾 spring；环线闭合 draw（自纠=环） ；`@draw` `@impulse` `@stagger` `@spring` |
| 3-D | p3-15..21 | **反事实：让它挑**：两部门对峙卡（登录派 vs 下单派）；「自动选」分支——热度柱长的错卡被推上王座（danger 红光），短柱对卡跌落；角标「本仓原型实测输出 · D4」 | 对峙卡 travel 相向；热度柱异步生长（count）；错卡登座 spring + 红光 breathe；对卡坠落 dim ；`@travel` `@count` `@spring` `@dim` |
| 3-E | p3-22..29 | **CONFLICT 卡片**：双卡并列、大问号、**数字区空白虚线框**（不给数字）；人手裁决灯落下定胜负；金句卡「我不懂的时候，我说我不懂」（serif，蓝图层的灵魂句） | 双卡 stagger 落位；空白框 draw；裁决灯 travel 落下 + impulse；金句卡 progress 淡入压轴 ；`@stagger` `@draw` `@travel` `@impulse` |

## P4 一个窗口 + 一个插头（p4-01..38）

| 镜 | 句区间 | 画面 | 动效 |
|---|---|---|---|
| 4-A | p4-01..08 | **唯一窗口**：三个 AI 图标（聊天气泡/代码/AI 报表机器人）排队走向同一个 activate 橙窗口；窗口收进「问题+身份」两张小卡，吐出上下文包（条目页+说明书+签名问答） | 三图标 stagger 排队 travel；窗口柜体 spring 落位；输入卡吸入、输出包弹出（travel 往返） ；`@stagger` `@travel` `@spring` |
| 4-B | p4-09..10 | **不装懂**：一个问题气泡落进窗口，目录墙扫过没有金标命中——窗口弹出显式警告条「此域无权威定义」（不静默），拒绝递猜测页 | 目录墙扫描线扫过（progress）；警告条 rise + impulse 黄闪一次 ；`@progress` `@enter:rise` `@impulse` |
| 4-C | p4-11..15 | **四因子天平**：四个砝码（相关/权威/常用/新鲜）落盘称重一叠页；权威砝码特写——manual 金「人写」块天然压秤重于 dig 紫「机器挖」块 | 四砝码 stagger 落盘（天平微倾动画）；出身对比块上下对置，金块下沉压弯秤杆（spring） ；`@stagger` `@spring` |
| 4-D | p4-16..20 | **对数封顶**：热度柱坐标系——线性刻度下爆款柱直冲顶棚压扁别人；切对数+封顶刻度后差距被压缩回合理区间；tie-break 三级判序小卡（分数→权威→新旧→名字） | 坐标轴 draw；线性柱 count 爆长压顶（shake）；刻度轴翻转为对数（impulse 转轴）柱高回落；判序三卡 stagger ；`@draw` `@count` `@shake` `@stagger` |
| 4-E | p4-21..24 | **USB-C 插头**：一枚 activate 橙通用插头从右滑入，插进「上下文层」机身——机身整排治理灯逐个点亮；不同 AI 设备图标（不同形状）逐一接上同一插口 | 插头 travel 滑入插入（snap spring）；治理灯 stagger 逐亮；三台异形设备依次接上（stagger）各自屏幕亮起 ；`@travel` `@spring` `@stagger` |
| 4-F | p4-25..28 | **四工具卡**：横排四张工具卡（列目录/问询/执行/反馈），每张配一个 10 帧微动画演示（过滤列表/递包/算数+二次验权/踩赞热度条） | 四卡 stagger 弹入；微动画各自 progress 演一遍；「执行」卡上的权限二次校验徽章 impulse 闪 ；`@stagger` `@progress` `@impulse` |
| 4-G | p4-29..34 | **原型实测**：终端风格输出面板滚动真实 selftest 行（T5 拒绝行 / T6 排序易位行），关键行 engine 高亮；右下角「本仓原型实测输出」常驻角标 | 输出行逐行上滚（progress 等速）；关键行到达时 impulse 高亮 + 定格 12 帧 ；`@progress` `@impulse` |
| 4-H | p4-35..38 | 收束帧：五块积木全景再次点亮——窗口（对内）与插头（对外）两块放大高亮，其余三块微光 | 五积木 stagger 复亮；窗口+插头两块 pushIn 放大 + activate 辉光；其余三块 dim 微光陪衬 ；`@stagger` `@pushIn` `@dim` |

## P5 治理的边界（p5-01..22）

| 镜 | 句区间 | 画面 | 动效 |
|---|---|---|---|
| 5-A | p5-01..05 | **477 vs 48**：四道全绿工序（定义✓公式✓权限✓）+ 最上游「预聚合」冒紫烟；477 与 48 两数字对撞裂开鸿沟；角标「第三方（Typedef）复现」 | 工序绿灯 stagger；紫烟 flowDash 升腾；数字对撞 travel + 裂缝 draw ；`@stagger` `@flowDash` `@travel` `@draw` |
| 5-B | p5-06..12 | **三级对策台阶**：最底级「注册期扫描」（算式卡被扫描线标出危险结构）→ 中级「出口对账」（签名答案 vs 重算值不齐 → 报警铃）→ 顶级「事后评测兜底」（考卷+血缘回溯网）；台阶逐级向上 | 三级台阶 stagger 升起（每级亮时演示其微动画：扫描 progress / 报警 impulse / 回溯网 draw） ；`@stagger` `@progress` `@impulse` `@draw` |
| 5-C | p5-13..14 | 出口脱敏 + 审计：回答气泡过出口扫描线，PII 块打码；旁边审计日志本逐行记下「谁/何时/什么身份/看了哪条」 | 扫描线横扫（progress）；打码格翻黑（stagger）；日志行打字机逐字（draw） ；`@progress` `@stagger` `@draw` |
| 5-D | p5-15..17 | 价值数字复引卡：24.1%→86.3% 翻牌 + 成本砍 2/3；右上红星号「厂商自家基准 · 你的基线自己测」常驻 | 数字 count 翻牌；星号角标 impulse 两闪后 breathe 低亮常驻 ；`@count` `@impulse` `@breathe` |
| 5-E | p5-18..21 | **通用化折损**：大厂楼（门禁焊在引擎墙里，一体浇筑）vs 自建层（门禁是后装的一道执行层闸机，位置变了）；金句收束「防线换了位置，纪律不能换」（serif） | 左楼剖面一体描线（draw 整体成形）；右侧闸机后装卡扣（spring 咔哒）；金句卡 progress 淡入 ；`@draw` `@spring` `@progress` |

## P6 路线图与诚实（p6-01..22）

| 镜 | 句区间 | 画面 | 动效 |
|---|---|---|---|
| 6-A | p6-01..05 | **P0 已完成**：原型卡片墙——六机制卡全绿勾 + 七次破坏实验卡（每张标注退化数字），右下「selftest 全绿」徽章；角标「本仓原型实测」 | 机制卡 stagger 翻绿；破坏实验卡 stagger 亮红角标再收绿（对照节奏）；徽章 spring 盖章 ；`@stagger` `@spring` |
| 6-B | p6-06..10 | **路线图四级台阶**：P0 实心亮（已完成）→ P1 最小服务 / P2 信任自纠 / P3 互操作三级虚线台阶向上，各级标签小字；P1 级旁验收小卡（真实客户端命中签名问答/越权被拒+审计） | 台阶逐级 draw 描线上升；P0 实心 impulse 点亮、P1–P3 虚线呼吸（breathe）；验收小卡 stagger 贴上 ；`@draw` `@impulse` `@breathe` `@stagger` |
| 6-C | p6-11..15 | **诚实两条**：两块天平卡——左「已完成：仅第零步」实心，右「路线图：P1–P3」虚线；第二卡「造多大看需求」：一两个 AI（小目录+窗口）vs 一群 AI（全五块）两档尺寸图 | 两卡对置 draw 分界线；左实心 spring 落定、右虚线 dash 流动（flowDash）；两档尺寸图 stagger 对比 ；`@draw` `@spring` `@flowDash` `@stagger` |
| 6-D | p6-16..21 | 金句收尾「含义被治理好之前，AI 的聪明都是租来的」（serif 全屏）→ 信源卡（pinned commit + 蓝图/原型路径 + 访问日期）→ 渐黑收尾（末 beat 全程） | 金句卡 progress 淡入 + 单帧 impulse；信源卡 rise；末句起亮度均匀降 0（渐黑窗口=beat 时长）＋片尾静默 ；`@progress` `@impulse` `@enter:rise` `@dim` |
