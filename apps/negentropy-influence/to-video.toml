# to-video skill 工作区级机制配置：skill 的 check_series.py 读工作区根本文件
# （文件/键缺失 = 空默认，即新工作区的干净初始态）。只放本工作区要对机制
# 说的内容——机制本身住在 skill 仓。

[check_series]
# 宿主仓库（negentropy 仓根）级受检面：规则 5 相对链接死链扫描的仓库侧延伸。
# 这两份文档链接进工作区内容（knowledge-map 机制链 / CHANGELOG 条目），整目录
# 迁移会一次性打断，故纳入受检面。glob 相对 git 仓库根解析（PROJECT = 工作区
# 最近含 .git 的祖先）。
project_globs = ["docs/.agents/knowledge-map.md", "CHANGELOG.md"]

# 规则 7「去站点化」的适用系列：观众可见层（口播/分镜/场景组件）禁课程站标识
# （learn.shareAI /「课程」/ s01–s20 章号）；self-evolution 系是论文型收尾，
# 不在列。系列 id 集是内容策略而非机制——规则 7/8 原为 skill 前身
# check_series.py 里的硬编码，2026-09 机制外置时随内容留在工作区（skill 不
# 携带任何具体系列的身份）。
course_series_ids = ["claude-code-explained"]

# 规则 8「下期卡同步」的适用系列：配了「系列身份卡 + 下期预告卡」统一收尾
# 装置的系列（Harness Engineering 五集）；self-evolution 系是旧版论文型收尾
# （无身份卡），对其执法是假报——装置在哪个系列落地，规则就管到哪。
next_card_series_ids = ["claude-code-explained"]

# ── 骨架合法偏离登记(to-video 2.0.0 起登记面住工作区,RSI-010)─────────────
# 自 skill 1.x assets/video-skeleton/skeleton.toml 原样搬迁(表名加 skeleton.
# 前缀;npmrc-inert-key 代随 .npmrc 退出受门作废未搬)。格式说明与档位语义见
# skill 仓 assets/video-skeleton/skeleton.toml;指纹取法:verify_skeleton.py 报告。
[[skeleton.generation]]
id = "bilingual-i18n"
reason = "双语渲染分代（2026-09-24，RSI-004）：Root/NarrationAudio/Subtitle/ChapterProgress/Main 加 lang 维度、新增 i18n.tsx；zh 渲染逐像素不变。撤销条件=花名册内各集下次重渲时按整组同步（五文件 + i18n.tsx 拷齐再 tsc），同步后从 roster 删除该集。"
episodes = [
  "claude-code-concurrency-video",
  "claude-code-explained-video",
  "claude-code-memory-video",
  "claude-code-multiagent-video",
  "claude-code-planning-video",
  "context-layer-video",
  "dream-rsi-video",
  "experience-era-agents-video",
  "horizon-context-video",
  "jev-decision-model-video",
  "openviking-video",
  "self-evolving-coding-agents-video",
  "self-improving-agents-video",
]

[skeleton.generation.legacy]
"video/src/Root.tsx" = "6554f6af763d"
"video/src/components/NarrationAudio.tsx" = "ebe05df884cf"
"video/src/components/Subtitle.tsx" = "7faa361ae76f"
"video/src/components/ChapterProgress.tsx" = "ca3d397fe1da"
"video/src/Main.tsx" = "4cb4e9be10a7" # regioned 归一化口径
"video/src/i18n.tsx" = "缺失"
[[skeleton.drift]]
episode = "self-improving-agents-video"
path = "video/src/components/cards.tsx"
fingerprint = "4bba8c190f21"
reason = "多一个 ChapterCard（39 行），仅本集 P2Brain.tsx 使用；迁出至 components/chapter.tsx 后可撤销本条"
[[skeleton.drift]]
episode = "horizon-context-video"
path = "video/src/components/cards.tsx"
fingerprint = "缺失"
reason = "v6 全屏独占改造（2026-09-20，ISSUE-190）：QuoteCard 系金句卡由 archify 图整镜接管，卡片层整文件退役；同系列与其余集仍在用，模板保留。撤销条件=本集重新需要卡片层时从模板复制回来（届时指纹即失配，须更新本条）"
[[skeleton.drift]]
episode = "claude-code-explained-video"
path = "video/package.json"
fingerprint = "19f8755b0ee0"
reason = "A/C 轨改造（2026-09，见 docs/research/video-production/160 号 §5.4/§5.3）：新增 @remotion/paths·shapes·three·lottie（4.0.512 同版钉死，remotion peer 精确匹配零升级）+ three·@react-three/fiber·lottie-web（3D/Lottie 桥的 peer 依赖须显式声明）+ @types/three（three 0.185 起 npm 包已不带类型）。EP2–5 采纳同款依赖（模板收敛）或本集回退改造后撤销本条"
[[skeleton.drift]]
episode = "claude-code-explained-video"
path = "video/remotion.config.ts"
fingerprint = "a37fa7bc90c3"
reason = "A 轨 @remotion/three 无头渲染强制要求 Chromium OpenGL 后端 = angle（一行新增 setChromiumOpenGlRenderer）。其余集跟进该行回模板收敛后撤销本条"
[[skeleton.drift]]
episode = "experience-era-agents-video"
path = "video/src/components/Subtitle.tsx"
fingerprint = "61df1a5e08e2"
reason = "字幕风格分代（2026-09-14）：模板已升级为渲染层剥除句尾「。」；本集为已发布成片，源码保持发布态。撤销条件=按 to-video.toml [[skeleton.generation]] bilingual-i18n 整组同步（Main + ChapterProgress + Subtitle + Root + NarrationAudio + i18n.tsx，拷齐再 tsc --noEmit），并重跑 build 派生 chapters.json；同步后从 generation roster 删除本集并撤销本条"
[[skeleton.drift]]
episode = "self-improving-agents-video"
path = "video/src/components/Subtitle.tsx"
fingerprint = "61df1a5e08e2"
reason = "字幕风格分代（2026-09-14）：模板已升级为渲染层剥除句尾「。」；本集为已发布成片，源码保持发布态。撤销条件=按 to-video.toml [[skeleton.generation]] bilingual-i18n 整组同步（Main + ChapterProgress + Subtitle + Root + NarrationAudio + i18n.tsx，拷齐再 tsc --noEmit），并重跑 build 派生 chapters.json；同步后从 generation roster 删除本集并撤销本条"
[[skeleton.drift]]
episode = "self-evolving-coding-agents-video"
path = "video/src/components/Subtitle.tsx"
fingerprint = "61df1a5e08e2"
reason = "字幕风格分代（2026-09-14）：模板已升级为渲染层剥除句尾「。」；本集为已发布成片，源码保持发布态。撤销条件=按 to-video.toml [[skeleton.generation]] bilingual-i18n 整组同步（Main + ChapterProgress + Subtitle + Root + NarrationAudio + i18n.tsx，拷齐再 tsc --noEmit），并重跑 build 派生 chapters.json；同步后从 generation roster 删除本集并撤销本条"
[[skeleton.drift]]
episode = "claude-code-explained-video"
path = "video/src/components/Subtitle.tsx"
fingerprint = "61df1a5e08e2"
reason = "字幕风格分代（2026-09-14）：模板已升级为渲染层剥除句尾「。」；本集为已发布成片，源码保持发布态。撤销条件=按 to-video.toml [[skeleton.generation]] bilingual-i18n 整组同步（Main + ChapterProgress + Subtitle + Root + NarrationAudio + i18n.tsx，拷齐再 tsc --noEmit），并重跑 build 派生 chapters.json；同步后从 generation roster 删除本集并撤销本条"
[[skeleton.drift]]
episode = "claude-code-planning-video"
path = "video/src/components/Subtitle.tsx"
fingerprint = "61df1a5e08e2"
reason = "字幕风格分代（2026-09-14）：模板已升级为渲染层剥除句尾「。」；本集为已发布成片，源码保持发布态。撤销条件=按 to-video.toml [[skeleton.generation]] bilingual-i18n 整组同步（Main + ChapterProgress + Subtitle + Root + NarrationAudio + i18n.tsx，拷齐再 tsc --noEmit），并重跑 build 派生 chapters.json；同步后从 generation roster 删除本集并撤销本条"
[[skeleton.drift]]
episode = "claude-code-memory-video"
path = "video/src/components/Subtitle.tsx"
fingerprint = "61df1a5e08e2"
reason = "字幕风格分代（2026-09-14）：模板已升级为渲染层剥除句尾「。」；本集为已发布成片，源码保持发布态。撤销条件=按 to-video.toml [[skeleton.generation]] bilingual-i18n 整组同步（Main + ChapterProgress + Subtitle + Root + NarrationAudio + i18n.tsx，拷齐再 tsc --noEmit），并重跑 build 派生 chapters.json；同步后从 generation roster 删除本集并撤销本条"
[[skeleton.drift]]
episode = "claude-code-concurrency-video"
path = "video/src/components/Subtitle.tsx"
fingerprint = "61df1a5e08e2"
reason = "字幕风格分代（2026-09-14）：模板已升级为渲染层剥除句尾「。」；本集为已发布成片，源码保持发布态。撤销条件=按 to-video.toml [[skeleton.generation]] bilingual-i18n 整组同步（Main + ChapterProgress + Subtitle + Root + NarrationAudio + i18n.tsx，拷齐再 tsc --noEmit），并重跑 build 派生 chapters.json；同步后从 generation roster 删除本集并撤销本条"
[[skeleton.drift]]
episode = "claude-code-multiagent-video"
path = "video/src/components/Subtitle.tsx"
fingerprint = "61df1a5e08e2"
reason = "字幕风格分代（2026-09-14）：模板已升级为渲染层剥除句尾「。」；本集为已发布成片，源码保持发布态。撤销条件=按 to-video.toml [[skeleton.generation]] bilingual-i18n 整组同步（Main + ChapterProgress + Subtitle + Root + NarrationAudio + i18n.tsx，拷齐再 tsc --noEmit），并重跑 build 派生 chapters.json；同步后从 generation roster 删除本集并撤销本条"

# 顶部进度条分代（2026-09-22）：模板 Main.tsx 归一化指纹 179be60ed605 为分代前
# 模板值（10 个既有集此前已收敛对齐、零历史登记——9 已发布 + context-layer-video
# 待审），与上方 Subtitle 分代同构；新 frozen 的 ChapterProgress.tsx 既有集尚无此
# 文件（旧脚手架期产物，Main 未挂载不影响渲染），按 cards.tsx 先例钉「缺失」哨兵。
# 本批与 Subtitle 批的撤销指引已并入 bilingual-i18n 分代（2026-09-24）：单独同步
# Main/ChapterProgress/Subtitle 任一文件都会因 i18n.tsx import 断链而 tsc 红，
# 须按上方 [[skeleton.generation]] 整组同步后一并撤销。
[[skeleton.drift]]
episode = "experience-era-agents-video"
path = "video/src/Main.tsx"
fingerprint = "179be60ed605"
reason = "顶部进度条分代（2026-09-22）：模板 Main.tsx 新增 ChapterProgress 挂载；本集为已发布成片，源码保持发布态。撤销条件=按 to-video.toml [[skeleton.generation]] bilingual-i18n 整组同步（Main + ChapterProgress + Subtitle + Root + NarrationAudio + i18n.tsx，拷齐再 tsc --noEmit），并重跑 build 派生 chapters.json；同步后从 generation roster 删除本集并撤销本条"
[[skeleton.drift]]
episode = "self-improving-agents-video"
path = "video/src/Main.tsx"
fingerprint = "179be60ed605"
reason = "顶部进度条分代（2026-09-22）：模板 Main.tsx 新增 ChapterProgress 挂载；本集为已发布成片，源码保持发布态。撤销条件=按 to-video.toml [[skeleton.generation]] bilingual-i18n 整组同步（Main + ChapterProgress + Subtitle + Root + NarrationAudio + i18n.tsx，拷齐再 tsc --noEmit），并重跑 build 派生 chapters.json；同步后从 generation roster 删除本集并撤销本条"
[[skeleton.drift]]
episode = "self-evolving-coding-agents-video"
path = "video/src/Main.tsx"
fingerprint = "179be60ed605"
reason = "顶部进度条分代（2026-09-22）：模板 Main.tsx 新增 ChapterProgress 挂载；本集为已发布成片，源码保持发布态。撤销条件=按 to-video.toml [[skeleton.generation]] bilingual-i18n 整组同步（Main + ChapterProgress + Subtitle + Root + NarrationAudio + i18n.tsx，拷齐再 tsc --noEmit），并重跑 build 派生 chapters.json；同步后从 generation roster 删除本集并撤销本条"
[[skeleton.drift]]
episode = "horizon-context-video"
path = "video/src/Main.tsx"
fingerprint = "179be60ed605"
reason = "顶部进度条分代（2026-09-22）：模板 Main.tsx 新增 ChapterProgress 挂载；本集为已发布成片，源码保持发布态。撤销条件=按 to-video.toml [[skeleton.generation]] bilingual-i18n 整组同步（Main + ChapterProgress + Subtitle + Root + NarrationAudio + i18n.tsx，拷齐再 tsc --noEmit），并重跑 build 派生 chapters.json；同步后从 generation roster 删除本集并撤销本条"
[[skeleton.drift]]
episode = "claude-code-explained-video"
path = "video/src/Main.tsx"
fingerprint = "179be60ed605"
reason = "顶部进度条分代（2026-09-22）：模板 Main.tsx 新增 ChapterProgress 挂载；本集为已发布成片，源码保持发布态。撤销条件=按 to-video.toml [[skeleton.generation]] bilingual-i18n 整组同步（Main + ChapterProgress + Subtitle + Root + NarrationAudio + i18n.tsx，拷齐再 tsc --noEmit），并重跑 build 派生 chapters.json；同步后从 generation roster 删除本集并撤销本条"
[[skeleton.drift]]
episode = "claude-code-planning-video"
path = "video/src/Main.tsx"
fingerprint = "179be60ed605"
reason = "顶部进度条分代（2026-09-22）：模板 Main.tsx 新增 ChapterProgress 挂载；本集为已发布成片，源码保持发布态。撤销条件=按 to-video.toml [[skeleton.generation]] bilingual-i18n 整组同步（Main + ChapterProgress + Subtitle + Root + NarrationAudio + i18n.tsx，拷齐再 tsc --noEmit），并重跑 build 派生 chapters.json；同步后从 generation roster 删除本集并撤销本条"
[[skeleton.drift]]
episode = "claude-code-memory-video"
path = "video/src/Main.tsx"
fingerprint = "179be60ed605"
reason = "顶部进度条分代（2026-09-22）：模板 Main.tsx 新增 ChapterProgress 挂载；本集为已发布成片，源码保持发布态。撤销条件=按 to-video.toml [[skeleton.generation]] bilingual-i18n 整组同步（Main + ChapterProgress + Subtitle + Root + NarrationAudio + i18n.tsx，拷齐再 tsc --noEmit），并重跑 build 派生 chapters.json；同步后从 generation roster 删除本集并撤销本条"
[[skeleton.drift]]
episode = "claude-code-concurrency-video"
path = "video/src/Main.tsx"
fingerprint = "179be60ed605"
reason = "顶部进度条分代（2026-09-22）：模板 Main.tsx 新增 ChapterProgress 挂载；本集为已发布成片，源码保持发布态。撤销条件=按 to-video.toml [[skeleton.generation]] bilingual-i18n 整组同步（Main + ChapterProgress + Subtitle + Root + NarrationAudio + i18n.tsx，拷齐再 tsc --noEmit），并重跑 build 派生 chapters.json；同步后从 generation roster 删除本集并撤销本条"
[[skeleton.drift]]
episode = "claude-code-multiagent-video"
path = "video/src/Main.tsx"
fingerprint = "179be60ed605"
reason = "顶部进度条分代（2026-09-22）：模板 Main.tsx 新增 ChapterProgress 挂载；本集为已发布成片，源码保持发布态。撤销条件=按 to-video.toml [[skeleton.generation]] bilingual-i18n 整组同步（Main + ChapterProgress + Subtitle + Root + NarrationAudio + i18n.tsx，拷齐再 tsc --noEmit），并重跑 build 派生 chapters.json；同步后从 generation roster 删除本集并撤销本条"
[[skeleton.drift]]
episode = "experience-era-agents-video"
path = "video/src/components/ChapterProgress.tsx"
fingerprint = "缺失"
reason = "顶部进度条分代（2026-09-22）：新 frozen 文件本集尚无（旧脚手架期产物）；Main 未挂载故不影响既有渲染。撤销条件=按 to-video.toml [[skeleton.generation]] bilingual-i18n 整组同步（Main + ChapterProgress + Subtitle + Root + NarrationAudio + i18n.tsx，拷齐再 tsc --noEmit），并重跑 build 派生 chapters.json；同步后从 generation roster 删除本集并撤销本条"
[[skeleton.drift]]
episode = "self-improving-agents-video"
path = "video/src/components/ChapterProgress.tsx"
fingerprint = "缺失"
reason = "顶部进度条分代（2026-09-22）：新 frozen 文件本集尚无（旧脚手架期产物）；Main 未挂载故不影响既有渲染。撤销条件=按 to-video.toml [[skeleton.generation]] bilingual-i18n 整组同步（Main + ChapterProgress + Subtitle + Root + NarrationAudio + i18n.tsx，拷齐再 tsc --noEmit），并重跑 build 派生 chapters.json；同步后从 generation roster 删除本集并撤销本条"
[[skeleton.drift]]
episode = "self-evolving-coding-agents-video"
path = "video/src/components/ChapterProgress.tsx"
fingerprint = "缺失"
reason = "顶部进度条分代（2026-09-22）：新 frozen 文件本集尚无（旧脚手架期产物）；Main 未挂载故不影响既有渲染。撤销条件=按 to-video.toml [[skeleton.generation]] bilingual-i18n 整组同步（Main + ChapterProgress + Subtitle + Root + NarrationAudio + i18n.tsx，拷齐再 tsc --noEmit），并重跑 build 派生 chapters.json；同步后从 generation roster 删除本集并撤销本条"
[[skeleton.drift]]
episode = "horizon-context-video"
path = "video/src/components/ChapterProgress.tsx"
fingerprint = "缺失"
reason = "顶部进度条分代（2026-09-22）：新 frozen 文件本集尚无（旧脚手架期产物）；Main 未挂载故不影响既有渲染。撤销条件=按 to-video.toml [[skeleton.generation]] bilingual-i18n 整组同步（Main + ChapterProgress + Subtitle + Root + NarrationAudio + i18n.tsx，拷齐再 tsc --noEmit），并重跑 build 派生 chapters.json；同步后从 generation roster 删除本集并撤销本条"
[[skeleton.drift]]
episode = "claude-code-explained-video"
path = "video/src/components/ChapterProgress.tsx"
fingerprint = "缺失"
reason = "顶部进度条分代（2026-09-22）：新 frozen 文件本集尚无（旧脚手架期产物）；Main 未挂载故不影响既有渲染。撤销条件=按 to-video.toml [[skeleton.generation]] bilingual-i18n 整组同步（Main + ChapterProgress + Subtitle + Root + NarrationAudio + i18n.tsx，拷齐再 tsc --noEmit），并重跑 build 派生 chapters.json；同步后从 generation roster 删除本集并撤销本条"
[[skeleton.drift]]
episode = "claude-code-planning-video"
path = "video/src/components/ChapterProgress.tsx"
fingerprint = "缺失"
reason = "顶部进度条分代（2026-09-22）：新 frozen 文件本集尚无（旧脚手架期产物）；Main 未挂载故不影响既有渲染。撤销条件=按 to-video.toml [[skeleton.generation]] bilingual-i18n 整组同步（Main + ChapterProgress + Subtitle + Root + NarrationAudio + i18n.tsx，拷齐再 tsc --noEmit），并重跑 build 派生 chapters.json；同步后从 generation roster 删除本集并撤销本条"
[[skeleton.drift]]
episode = "claude-code-memory-video"
path = "video/src/components/ChapterProgress.tsx"
fingerprint = "缺失"
reason = "顶部进度条分代（2026-09-22）：新 frozen 文件本集尚无（旧脚手架期产物）；Main 未挂载故不影响既有渲染。撤销条件=按 to-video.toml [[skeleton.generation]] bilingual-i18n 整组同步（Main + ChapterProgress + Subtitle + Root + NarrationAudio + i18n.tsx，拷齐再 tsc --noEmit），并重跑 build 派生 chapters.json；同步后从 generation roster 删除本集并撤销本条"
[[skeleton.drift]]
episode = "claude-code-concurrency-video"
path = "video/src/components/ChapterProgress.tsx"
fingerprint = "缺失"
reason = "顶部进度条分代（2026-09-22）：新 frozen 文件本集尚无（旧脚手架期产物）；Main 未挂载故不影响既有渲染。撤销条件=按 to-video.toml [[skeleton.generation]] bilingual-i18n 整组同步（Main + ChapterProgress + Subtitle + Root + NarrationAudio + i18n.tsx，拷齐再 tsc --noEmit），并重跑 build 派生 chapters.json；同步后从 generation roster 删除本集并撤销本条"
[[skeleton.drift]]
episode = "claude-code-multiagent-video"
path = "video/src/components/ChapterProgress.tsx"
fingerprint = "缺失"
reason = "顶部进度条分代（2026-09-22）：新 frozen 文件本集尚无（旧脚手架期产物）；Main 未挂载故不影响既有渲染。撤销条件=按 to-video.toml [[skeleton.generation]] bilingual-i18n 整组同步（Main + ChapterProgress + Subtitle + Root + NarrationAudio + i18n.tsx，拷齐再 tsc --noEmit），并重跑 build 派生 chapters.json；同步后从 generation roster 删除本集并撤销本条"
[[skeleton.drift]]
episode = "dream-rsi-video"
path = "video/src/components/ChapterProgress.tsx"
fingerprint = "4e79c14a211a"
reason = "停在 2026-09-22 分代之前的更早版本（该集晚于此文件冻结清单建立、早于 09-22 模板收敛，09-22 批次登记了「缺失」集却漏了这两个「有而更旧」的集）。撤销条件=按 to-video.toml [[skeleton.generation]] bilingual-i18n 整组同步（Main + ChapterProgress + Subtitle + Root + NarrationAudio + i18n.tsx，拷齐再 tsc --noEmit），并重跑 build 派生 chapters.json；同步后从 generation roster 删除本集并撤销本条"
[[skeleton.drift]]
episode = "context-layer-video"
path = "video/src/components/ChapterProgress.tsx"
fingerprint = "4e79c14a211a"
reason = "停在 2026-09-22 分代之前的更早版本（同 dream-rsi 条目成因；双语分代使其从「被模板锚遮蔽」转为显形，此处补齐 09-22 批次遗漏的登记）。撤销条件=按 to-video.toml [[skeleton.generation]] bilingual-i18n 整组同步（Main + ChapterProgress + Subtitle + Root + NarrationAudio + i18n.tsx，拷齐再 tsc --noEmit），并重跑 build 派生 chapters.json；同步后从 generation roster 删除本集并撤销本条"

# 已撤销（2026-08）：`Main.tsx` 的两条登记。建门时那两集的 SceneFade 块落后于模板
# 一处注释扩写与换行调整，当时刻意只登记不收敛（收敛要动两个**已发布**集，属独立
# 决策）。现已对齐到模板并删除登记 —— 逃逸口用完即收，否则登记表会单调增长成
# 「历史遗留清单」，而它本该是「当前合法偏离的完整枚举」。
# 收敛判据：归一化 diff 证明差异仅为注释文字与 JSX 属性折行，三个 props 表达式
# 逐字相同 ⇒ 渲染输出零变化，两集**无需重渲**。
