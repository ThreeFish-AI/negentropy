# Negentropy Environment Configuration Template
# Copy this file to `.env` and fill in your values.
# NEVER commit the actual `.env` file to version control.

# =============================================================================
# ArtifactService Configuration
# =============================================================================
# 后端类型：inmemory (开发) | gcs (Google Cloud Storage)
ARTIFACT_SERVICE_BACKEND=inmemory
GCS_BUCKET_NAME=your-gcs-bucket-name

# Authentication (Explicit key file or Application Default Credentials).
# For GCS, ensure your environment has GOOGLE_APPLICATION_CREDENTIALS set
# or that your service account has appropriate permissions.

# =============================================================================
# LLM Configuration
# =============================================================================
# 默认使用的大语言模型，格式：provider/model_name
# 参考：https://docs.litellm.ai/docs/providers
DEFAULT_MODEL=openai/glm-4.7

# =============================================================================
# Database Configuration
# =============================================================================
# PostgreSQL 连接字符串 (asyncpg 驱动)
# 格式：postgresql+asyncpg://user:password@host:port/database
DATABASE_URL=postgresql+asyncpg://aigc:@localhost:5432/negentropy

# 连接池大小
DB_POOL_SIZE=5
# 最大溢出连接数
DB_MAX_OVERFLOW=10
# 连接回收时间（秒）
DB_POOL_RECYCLE=3600
# 是否打印 SQL 日志（调试用）
DB_ECHO=false

# =============================================================================
# MemoryService Configuration
# =============================================================================
# 后端类型：inmemory (开发) | vertexai (GCP) | postgres (PostgreSQL)
MEMORY_SERVICE_BACKEND=inmemory

# =============================================================================
# SessionService Configuration
# =============================================================================
# 后端类型：inmemory (开发) | vertexai (GCP) | database (ADK) | postgres (PostgreSQL/SQLite)
SESSION_SERVICE_BACKEND=inmemory

# VertexAI 配置 (当使用 vertexai 后端时必填)
# VERTEX_PROJECT_ID=your-gcp-project-id
# VERTEX_LOCATION=us-central1
# VERTEX_AGENT_ENGINE_ID=1234567890
