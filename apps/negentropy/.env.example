# Negentropy Environment Configuration Template
# Copy this file to `.env` and fill in your values.
# NEVER commit the actual `.env` file to version control.

# =============================================================================
# LLM Configuration
# =============================================================================
# 默认使用的大语言模型，格式：provider/model_name
# 参考：https://docs.litellm.ai/docs/providers
DEFAULT_MODEL=openai/glm-4.7

# =============================================================================
# Database Configuration
# =============================================================================
# PostgreSQL 连接字符串 (asyncpg 驱动)
# 格式：postgresql+asyncpg://user:password@host:port/database
DATABASE_URL=postgresql+asyncpg://aigc:@localhost:5432/negentropy

# 连接池大小
DB_POOL_SIZE=5
# 最大溢出连接数
DB_MAX_OVERFLOW=10
# 连接回收时间（秒）
DB_POOL_RECYCLE=3600
# 是否打印 SQL 日志（调试用）
DB_ECHO=false
