# Changelog

本文件遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 约定，版本号遵循 [SemVer](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### Changed（破坏性变更）

- **[BREAKING] Catalog 全局化三阶段重构（corpus_id → catalog_id）**：
  - `doc_catalog_nodes` 与 `doc_catalog_memberships` 表已被 `doc_catalog_entries`（N:M 全局关联表）替代；`doc_catalogs` 成为独立的全局顶层实体，不再依附 Corpus。
  - `wiki_publications.corpus_id` 列已移除，替换为 `catalog_id` + `app_name` + `publish_mode`（`live` | `snapshot`）+ `visibility`；`UNIQUE(corpus_id, slug)` 约束已更换为 `UNIQUE(catalog_id, slug)`。
  - 所有创建 Wiki Publication 的 API 请求需将 `corpus_id` 字段替换为 `catalog_id`；`GET /wiki/publications` 的 `corpusId` 查询参数同样变更为 `catalogId`。
  - 前端 BFF 代理路由：旧版 `GET /api/knowledge/catalog/tree/{corpusId}` 已返回 `410 Gone`；请改用 `GET /api/knowledge/catalogs/{catalogId}/tree`。
  - `CatalogDao.create_node` / `get_tree` / `assign_document` / `unassign_document` 等方法的 `corpus_id` 参数已替换为 `catalog_id` 或 `catalog_entry_id`，详见 [catalog_dao.py](../apps/negentropy/src/negentropy/knowledge/catalog_dao.py)。

  **迁移指南**：
  1. 执行数据库迁移：`cd apps/negentropy && uv run alembic upgrade head`（三阶段 Revision 0003→0004→0005 顺序应用）。
  2. 若需降级，先检查是否存在跨 Corpus 的 Catalog（Revision 0005 downgrade 守卫会拒绝含跨 corpus entries 的降级并抛 `RuntimeError`）。
  3. 前端：将所有 `corpusId` props / state / API 参数重命名为 `catalogId`，并确认 BFF 代理已对齐新路由（`/catalogs/` 前缀）。
  4. Wiki SSG：`WikiPublication` 接口的 `corpus_id` 字段已替换为 `catalog_id`；`publish_mode` 字段新增。

### Fixed

- 修复 Pipeline Run 卡在 RUNNING 状态不收敛的问题（如 `rebuild_source-7e5194ae` 任务实际已完成但 UI 始终显示 RUNNING）：根因为所有 `execute_*_pipeline` 后台任务方法仅使用 `try/except Exception`，缺少 `finally` 安全网——当异常处理链自身失败（如 `_fail_pipeline_execution` 中 DB 写入失败）或后台任务被取消（`asyncio.CancelledError` 是 `BaseException`，不被 `except Exception` 捕获）时，`PipelineTracker` 状态永远停留在 running。修复：为 `PipelineTracker` 新增幂等安全网方法 `ensure_finalized()`（已终态则 noop，未终态则强制写入 failed），并在全部 8 个 `execute_*_pipeline` 方法中追加 `finally` 块调用该安全网；同时将 4 个方法的 `raise` 统一改为 `return []`（与其它 4 个方法一致，避免 uvicorn 打印异常堆栈）。在 DAO 层新增 `finalize_stale_pipeline_runs()` 批量回收方法，将超时 running 记录标记为 failed。`service.py` / `dao.py` / `test_pipeline_tracker.py` 变更。
- 修复 `/knowledge/catalog` 页面**未选择目录**时点击「添加根节点」→ 填写并「创建」触发 `POST http://localhost:3192/api/knowledge/catalogs/entries → 405 Method Not Allowed`（前端报错 `Failed to create catalog node: Method Not Allowed`）：根因为**前端入口门禁缺失叠加 URL 模板字符串空段降级**——`app/knowledge/catalog/page.tsx:12` 中 `catalogId` 初值为 `null`，而左侧 aside 在 `catalogId` 未选时仍无条件渲染 `CatalogTree`，致使 ISSUE-001（commit `731894b`）为修复「空态吞主操作」而改为恒常可见的「添加根节点」按钮在**未选 Catalog** 语境下也暴露入口；`CreateNodeDialog catalogId={catalogId ?? ""}` 强制将 `null` 降级为 `""`，`features/knowledge/utils/knowledge-api.ts:createCatalogNode` 的 `` fetch(`/api/knowledge/catalogs/${catalog_id}/entries`) `` 模板字符串遂产出 `/api/knowledge/catalogs//entries`，经 Next.js App Router 路径归一化合并连续 `//` 后等效命中 `app/api/knowledge/catalogs/[catalogId]/route.ts`（`catalogId="entries"`），该文件仅导出 `GET/PATCH/DELETE` → 405。ISSUE-001 修复时把「空态隐藏主操作」当作入口缺失修复，**未区分 `nodes=[]` 与 `catalogId=null` 两个正交维度**，埋下本次缺陷伏笔；本次问题纯前端入口 / 参数空值漏判，后端 `api.py` 与 BFF `/catalogs/[catalogId]/entries/route.ts` 契约与实现均正确。修复（**最小干预 + Defense in Depth**）：Layer 1（UX 入口门禁 / 主修复）—— `page.tsx` aside 条件渲染补齐 `!catalogId` 分支，渲染「请先选择目录」dashed-border 空态占位并屏蔽 `CatalogTree`，「添加根节点」按钮自然不可达；保留「已选 Catalog 但 `nodes=[]`」时 `CatalogTree` 内部的空态入口（ISSUE-001 语义保持）。Layer 2（API 客户端前置校验 / 纵深防御）—— `createCatalogNode` 入口增加 `if (!catalog_id) throw new Error("catalog_id is required to create a catalog node")` 守卫，把「URL 归一化后静默漂移」这类低可观测性缺陷前置为显式错误，防范未来其他入口重蹈覆辙；其它 Catalog API 函数（`fetchCatalogNodes` / `updateCatalogNode` / `deleteCatalogNode` / `fetchCatalogNode` / `fetchCatalogNodeDocuments` / `assignDocumentToNode` / `unassignDocumentFromNode`）暂不扩散改造（仅在已选 Catalog 且已选 Node 前提下可达，现阶段风险低），遵循最小干预原则。Layer 3（回归测试）—— `tests/unit/knowledge/knowledge-api.test.ts` 新增 2 条用例：空 `catalog_id` 抛错且不发起 `fetch`（调用次数断言 0）、合法 UUID 精确命中 `/api/knowledge/catalogs/<uuid>/entries` 且 body 不含 `catalog_id`。后端 `apps/negentropy/src/negentropy/knowledge/api.py` / BFF 代理 `app/api/knowledge/catalogs/*` / `CatalogTree.tsx` / `CatalogTreeNode.tsx` / `CreateNodeDialog.tsx` 零改动。防范：在 `docs/issue.md` 追加 ISSUE-014 沉淀「URL 空路径段 + 动态路由归一化」跨上下文教训，并在 ISSUE-001 末尾追加 2026-04-24 回归事件，记录「空态修复应同时区分 `nodes=[]` 与 `parent_context=null` 两个正交维度」的复盘。
- 修复 Knowledge Base → Corpus Settings 页面侧边栏「Documents」「← Back」按钮点击无响应，以及从其他页面（如 Knowledge Graph）返回 Knowledge Base 时仍停留在 Settings 视图而非概览页的导航失效缺陷：根因为 `syncQueryState` 使用 Next.js `router.replace()` 更新同路径 URL query params，但 Next.js App Router 客户端路由在同路径替换时存在去重/缓存机制，`searchParams` 对象引用不变导致 `useEffect([searchParams])` 不重新触发状态同步。修复：将 `router.replace()` 替换为 `window.history.replaceState()` + 直接同步 React 状态（`setViewMode` / `setSelectedCorpusId` / `setCorpusTab` / `setSelectedDocumentId`），绕过 Next.js 路由缓存；同时将 `useE[searchParams]`（对象引用）改为 `[searchParams.toString()]`（字符串内容比较），确保跨页面返回时能正确触发状态重置至概览模式。移除不再使用的 `useRouter` 引用。前端其他组件、后端 API 零改动。

- 修复新建 Corpus 后 Settings Tab 的 **Document Extraction Settings** 面板「URL 文档」与「PDF 文档」主/备下拉框全部显示「未配置」的可观测缺陷（预期应预置 `negentropy-perceives` 的 4 个默认提取工具 `parse_webpage_to_markdown` / `parse_webpages_to_markdown` / `parse_pdf_to_markdown` / `parse_pdfs_to_markdown`）。根因为**预置数据与依赖它的应用逻辑未对称交付**：`config/knowledge.py:DefaultExtractorRoutesSettings` 已声明 4 个 `(server_name, tool_name)` 默认对，`knowledge/api.py:create_corpus()` 也正确地在请求未显式传入 `extractor_routes` 时调用 `_resolve_default_extractor_routes()`（`api.py:541-625`），但该解析器以刚性外键语义查询 `mcp_tools` 表验证 4 行存在且 `is_enabled=TRUE`——而迁移 `0002_seed_negentropy_perceives.py` 仅幂等 upsert 了 MCP **Server**，未同步预置 4 个 MCP **Tool** 行；Tool 行的唯一落地路径是管理员进入 `/interface/mcp` 手动点击「Load Tools」触发 `POST /interface/mcp/servers/{id}/tools/load` → `McpClientService.discover_tools()` 通过 HTTP 连接 `http://localhost:2992/mcp` 的 live discovery（`interface/api.py:700-760`）；因此全新部署执行 `alembic upgrade head` 后，用户（尤其本地开发者在 `negentropy-perceives` 尚未启动、或未手动点 Load Tools 的情况下）首次创建 Corpus 会稳定命中 `_resolve_default_extractor_routes()` 的空 `targets` 退化路径，仅在日志中留下 `knowledge_default_extractor_tool_not_found` WARN。修复（最小干预 + 正交分解）：新增 Alembic 迁移 `0006_seed_negentropy_perceives_tools.py`，与 `0002` 的「预置 Server（纯 DML）」语义对称承载「预置 Tool（纯 DML）」，单条 `INSERT ... SELECT s.id, ... FROM mcp_servers s CROSS JOIN (VALUES ...) WHERE s.name = 'negentropy-perceives' ON CONFLICT (server_id, name) DO UPDATE SET title=EXCLUDED.title, description=EXCLUDED.description, is_enabled=EXCLUDED.is_enabled, updated_at=now()` 幂等 upsert 4 行（仅写 `server_id` / `name` / `title` / `description` / `is_enabled`，JSONB 结构字段 `input_schema` / `output_schema` / `icons` / `annotations` / `execution` / `meta` 交由 `0001` 中已声明的 `server_default`（`{}`/`[]`）兜底，后续 live discovery 的 UPDATE 分支 `interface/api.py:730-741` 会按真实 schema 覆盖，不冲突）；`downgrade()` 精准回收这 4 行而不触碰 schema，并以 `server_id IN (SELECT id FROM mcp_servers WHERE name = 'negentropy-perceives')` 限定影响面。`_resolve_default_extractor_routes()` / `create_corpus()` / 前端 `normalizeExtractorDraftRoutes()` 零改动；已存在的 Corpus 不做 backfill（超出用户诉求范围）。Stairway 集成测试 `tests/integration_tests/db/test_migrations.py::test_migrations_stairway` 自动覆盖 `upgrade→downgrade→upgrade` 回环、验证幂等与对称性。防范：在 `docs/issue.md` 追加 ISSUE-013 沉淀「预置数据应与依赖它的应用逻辑一起交付」的跨上下文教训——当应用层逻辑对某张表的存在性/数据完整性做刚性校验（外键、UNIQUE、`is_enabled` 过滤）时，配套种子迁移需与应用层变更同批交付，不能把数据落地寄托于运行时 live discovery 等非确定性通路。
- 修复 Catalog「添加文档到节点」对话框只显示 1 条且文件名渲染为空的可观测缺陷（同一 corpus 下 Documents 页实际已有 3 份活跃文档），以及节点侧「归属文档」数量始终 0 的二阶漂移。根因一：`GET /knowledge/catalogs/{catalog_id}/documents` 端点语义与前端契约**反向**——后端实现为查询 `DocCatalogEntry.document_id WHERE catalog_id=:cid`（即「已归属文档」），而 `AddDocumentsDialog.tsx` 与 `fetchCatalogDocuments` JSDoc 明确声明的契约是「Catalog 作用域下可分配候选文档（跨 corpus；已归属由 UI 侧基于 `existingDocIds` prop 灰出）」，语义倒置直接导致对话框候选集退化为「已归属集」；根因二：两处 handler 的响应均以内联 dict 构造并用 `"filename"` 键（漏 `app_name` / `file_hash` / `gcs_uri` / `content_type` / `file_size` / `created_by` / `markdown_extract_status` 等字段），前端 `KnowledgeDocument` 接口按 `original_filename` 读 → `undefined` → UI 渲染空文件名；此外 `get_entry_documents` 响应外壳键误写为 `"items"`（前端 `CatalogNodeDocumentsResponse` 读 `.documents`），致 `DocumentAssignmentSection` 常显 0。commit `3b88bec` 补齐 Phase 3 `/catalogs/*` RESTful 路由时以内联 dict 构造响应，未复用 ISSUE-010（commit `2685ac3`）沉淀的 `_build_document_response()` 序列化 SSOT，属同型回归。修复：`get_catalog_documents` 改为加载 `DocCatalog` → 取 `app_name` → 复用 `DocumentStorageService.list_documents(app_name=…)` + `_build_document_response()`，返回完整 `DocumentResponse[]`，默认 `limit` 50→200 对齐前端一次加载；`get_entry_documents` 同步改用 `_build_document_response()`，响应外壳键从 `items` 迁回 `documents` 以对齐前端 `CatalogNodeDocumentsResponse`（「已归属」语义不变）。前端 `AddDocumentsDialog` / `DocumentAssignmentSection` / `knowledge-api.ts` 契约零改动。新增 5 个集成用例（跨 app 隔离、软删过滤、未知 catalog 404 + `CATALOG_NOT_FOUND` code、响应字段完整性、归档 catalog 行为）锁定契约；未知 catalog_id 从「静默返回空」升为 HTTP 404 属语义澄清型行为变更。防范：在 `docs/issue.md` ISSUE-010 追加 2026-04-24 回归事件并沉淀强制复用规则——后续返回 `KnowledgeDocument` 或其列表的端点一律复用 `_build_document_response()` + `DocumentResponse` pydantic schema，严禁在 route handler 内以内联 dict 构造。
- 修复 Alembic 迁移 `0005_catalog_global_phase3_enforce` 的 `downgrade` 路径在存量数据上直接抛 `asyncpg.UndefinedFunctionError: function lower(negentropy.catalogentrynodetype) does not exist`，以及紧随其后的 `ForeignKeyViolationError: insert or update on table "doc_catalog_memberships" violates foreign key constraint ... catalog_node_id`：前者根因为 PostgreSQL 的 `lower(text)` / `upper(text)` 等 text-only 标量函数对枚举类型无重载、且 `=` / `IN` 的隐式 text 强制转换不适用于函数调用——在 prepare/plan 阶段即失败，即使 `doc_catalog_entries` 为空也会触发；`doc_catalog_entries.node_type` 在 0003 建为原生枚举 `negentropy.catalogentrynodetype`，而 0005 downgrade 重建的 legacy 表 `doc_catalog_nodes.node_type` 为 `VARCHAR(20) NOT NULL DEFAULT 'category'`，二者类型错配。修复：在 `0005_catalog_global_phase3_enforce.py:223` 将 `LOWER(e.node_type)` 显式改写为 `LOWER(e.node_type::text)`——与 0004 upgrade `UPPER(n.node_type)::negentropy.catalogentrynodetype` 形成对称 cast 链路（enum ⇌ text）。后者根因为 Phase 3 DROP legacy 表之后应用层新增了「DOCUMENT_REF 作为另一个 DOCUMENT_REF 的子节点」这种前向语义（legacy schema 仅允许文档挂载在 CATEGORY/COLLECTION 下），回填 `doc_catalog_memberships` 的 SELECT 未与 `doc_catalog_nodes` JOIN，使不可表达的 parent_entry_id 穿透到 FK 校验；修复：在 step 3c 的 SELECT 上加 `JOIN negentropy.doc_catalog_nodes n ON n.id = e.parent_entry_id`，与 0004 upgrade step 3（`membership → node → catalog` 三连 JOIN）保持严格对称，legacy 无法表达的嵌套关系被显式过滤（与跨 corpus 守卫同构的「不可表达即不可回填」语义）。集成测试 `tests/integration_tests/db/test_migrations.py::test_migrations_stairway`（L42-55）的 `upgrade→downgrade→upgrade` 回环即天然回归闸门，修复前必红修复后必绿；全仓 `grep -E "LOWER|UPPER|INITCAP\\s*\\(\\s*[a-z_]+\\.[a-z_]*type"` 确认仅此两处命中且均安全。ORM 层（`perception.py` / `catalog_dao.py`）与前端零改动。
- 修复 Catalog 页面点击「创建」目录节点时返回 `Failed to create catalog node: method not allowed`：根因为 Phase 3 前端/BFF 全局化（commit `89f290a`）已迁移到新 RESTful 路径 `POST /knowledge/catalogs/{catalogId}/entries`，但后端 `api.py` 仍仅保留旧路由 `POST /catalog/nodes?catalog_id=`，15 条 `/catalogs` 前缀路由全部缺失，导致 BFF 代理转发时后端返回 405/404。修复：在后端 `api.py` 新增 15 条 `/catalogs` RESTful 路由（Catalog CRUD 5 条 + Tree 1 条 + Entry CRUD 5 条 + Documents 4 条），同时删除旧 `/catalog/nodes/*` 系列路由（10 条）及无调用者的 `get_document_catalog_nodes`、`get_catalog_subtree`、`move_catalog_node`；前端 `knowledge-api.ts` 中 7 个仍调用旧 BFF 路径的函数（`fetchCatalogNodes`、`fetchCatalogNode`、`updateCatalogNode`、`deleteCatalogNode`、`fetchCatalogNodeDocuments`、`assignDocumentToNode`、`unassignDocumentFromNode`）迁移至新路径并补充 `catalogId` 参数；3 个 UI 组件（`NodeDetailPanel`、`AddDocumentsDialog`、`DocumentAssignmentSection`）调用方签名同步适配；删除旧 BFF 代理目录 `app/api/knowledge/catalog/`；集成测试 `knowledge-catalog-route.test.ts` 更新为新路由。Service 层与 Schema 复用不改。
- 修复 `apps/negentropy-wiki` `pnpm build` / `pnpm start` 三类联动异常，使 Wiki 发布链路（UI 在线发布 → 5 分钟 ISR 自动回刷）在本地/容器运行时的可观测语义与 Negentropy UI 端到端对齐：
  - **`pnpm start` 监听 3000 而非约定的 3092**：根因为 `apps/negentropy-wiki/package.json:8` 的 `start` 脚本直接裸调 `node .next/standalone/server.js`，未注入 `PORT` / `HOSTNAME`，而 Next.js standalone 产物在 `process.env.PORT` 缺省时硬编码 3000；同仓 `apps/negentropy-ui/scripts/start-production.mjs:80-81` 早已建立 `PORT ??= "3192"; HOSTNAME ??= "localhost"` 的注入范式，wiki 侧未沿用 → 双应用 start 语义分叉（熵源）。复用 UI SSOT，新增 `apps/negentropy-wiki/scripts/start-production.mjs` 逐行复刻 `apps/negentropy-ui/scripts/start-production.mjs:1-122` 仅替换两处（`PORT ??= "3092"`、临时目录前缀 `negentropy-wiki-runtime-`），并把 `package.json:8` 改为 `node ./scripts/start-production.mjs`。仅 2 个 Next.js 应用暂不抽取 `packages/next-standalone-runner` 以免 YAGNI。
  - **启动即报 `[Wiki] Failed to load entry "_next/static/css/*.css": [TypeError: fetch failed]`**：根因链为 Next.js `output: "standalone"`（`next.config.ts:7`）产出 `.next/standalone/server.js` 时**不**自动拷贝 `.next/static/` 与 `public/`（官方文档明示需手动处理），standalone server 内部 `process.chdir(__dirname)` 把 cwd 切至 `.next/standalone/`，查不到 `.next/static/*.css` 后请求被 catch-all 动态路由 `src/app/[pubSlug]/[...entrySlug]/page.tsx` 解构为 `pubSlug="_next"` / `entrySlug=["static","css","..."]`，进而触发 `wikiApi.findPublicationBySlug("_next")` → `fetch(http://localhost:8000/...)` → 后端未运行 `ECONNREFUSED` → 堆叠为触目的 error 噪声。新 wrapper 脚本通过临时目录 + `linkRuntimeAsset()`（先 symlink 再 `cpSync` 回退）把 `.next/static/` 与 `public/` 布置到 standalone 邻近路径，让 Next.js 内建静态中间件命中、绕过动态路由回捞；运行时 `.temp/negentropy-wiki-runtime-*` 在 SIGINT/SIGTERM/退出时统一 cleanup，避免累积。
  - **`pnpm build` 终端打印 ERROR 级 `[Wiki] Failed to fetch publications: ECONNREFUSED`（伪失败观感）**：根因为 `src/app/page.tsx:18` 在 SSG 预渲染阶段把兜底 catch 的日志以 `console.error` 输出，与兄弟路由 `[pubSlug]/generate.ts:16`、`[pubSlug]/[...entrySlug]/generate.ts:26,32`、`[pubSlug]/[...entrySlug]/page.tsx:66` 四处的 `console.warn` 风格不一致；行为上 `publications = []` 的兜底 + 页面级 `export const revalidate = 300` ISR 回灌契约无需更改，仅降级日志等级并在文案中补注「后端不可达时回落为空列表，由 ISR 在首次请求时自愈」，与 UI → Wiki 发布链路的「5 分钟自动回刷」语义契合。
  - **`docs/negentropy-wiki-ops.md` 双源漂移同步修复**：作为 Wiki 运维 SSOT，其 §5.2 本地预览仍写「访问 http://localhost:3000」、§5.3 Dockerfile 示例 `EXPOSE 3000` + `CMD ["node", "server.js"]`、§8.1 故障排除把本属 WARN 的构建期日志列为「构建失败」——三处在本次修复后均从「事实」退化为「已过时」。同步更新 §5.2 端口表述与 wrapper 脚本注释、§5.3 `EXPOSE 3092` + `ENV PORT=3092 HOSTNAME=0.0.0.0`（容器内已 `COPY --from=builder` 布置静态资产，与 wrapper 脚本 symlink 同构，故保留 `CMD ["node", "server.js"]`）、§8.1 改写为「构建期告警」并附注 ISR 自愈窗口。
  - **Release CI 与 ISR 发布链路零扰动**：本次**未**将 wiki 纳入 `.github/workflows/negentropy-release.yml` 的 artifact 打包（当前仅 backend wheel + UI tgz），避免爆炸半径外溢；`src/app/**` 控制流、`src/lib/wiki-api.ts`、`next.config.ts` 的 `output: "standalone"` / `rewrites` / `revalidate: 300` 均保持原样，UI → `POST /knowledge/wiki/publications/{id}/publish` → DB → ISR 5 分钟回刷的发布契约零改动。
- 修复 `/knowledge/catalog` 页点击「添加根节点」创建时 `POST /api/knowledge/catalog` 返回 `{"detail":"Not Found"}`（前端报错 `Failed to create catalog node: Not Found`），以及同批次 Catalog / Wiki 全链路代理静默失效：根因为 `apps/negentropy-ui/app/api/knowledge/` 下 Catalog（5 个）+ Wiki（8 个）共 **13 个 BFF 代理** 的 `path` 参数漏写 `/knowledge` 前缀（如误写 `/catalog/nodes` / `` `/wiki/publications/${pubId}/publish` ``），而 `knowledge/_proxy.ts` 的 `new URL(path, baseUrl)` 不在代理层拼接域前缀——后端 `APIRouter(prefix="/knowledge")` 实际暴露的是 `/knowledge/catalog/**` / `/knowledge/wiki/**`，导致 FastAPI 直接 404；对照 Memory 域 14 条、Interface 域 22 条、Knowledge 域既有 17 条正确路由均遵循「path 自带域前缀」的跨域约定，本批漂移孤立。同时发现二阶不一致：后端 `create_catalog_node(body: _CatalogNodeCreateReq, corpus_id: UUID = Query(...))` 将 `corpus_id` 声明为 Query 参数，而前端 `createCatalogNode` 将其放在 body 里，路径修复后将触发 422——本次一并将 `corpus_id` 从 body 剥离至 query 以对齐后端 Pydantic/FastAPI 契约。三阶跨域不对称：Knowledge / Memory / Interface 三个 `_proxy.ts` 的 `proxyGet` / `proxyDelete` / `proxyGetBinary` 均显式转发 `incomingUrl.search`，但 `proxyPost` / `proxyPostFormData` / `proxyPatch` 历来遗漏该步骤（此前无「POST + Query 参数」组合使用而未暴露）；本次将 search 转发补齐到三域 6 个非 GET/DELETE 代理函数，结构对称、空 search 恒等 no-op 不影响任何既有调用，彻底消除未来新增 Query-参数化 POST/PATCH 端点再次踩坑的风险面。影响面覆盖 Catalog 根/子节点 CRUD、节点文档分配/移除、Wiki 发布列表 CRUD、publish / unpublish / entries / nav-tree / sync-from-catalog / entry content 全部受影响接口；既有 `base/*` / `dashboard` / `documents` / `graph` / `pipelines` / `stats` / `search` 等 17 条合规路由零改动。防范：在 `apps/negentropy-ui/app/api/knowledge/_proxy.ts` 顶部沉淀 SSOT 头注释（列出正反例、指向 Memory/Interface 同构参照），并新增 `tests/integration/knowledge-catalog-route.test.ts` mock-fetch 单测断言 `POST` 精确落到 `http://localhost:3292/knowledge/catalog/nodes?corpus_id=...`，把「路径漂移」与「search 未转发」这两类低级错误前移到 CI。后端路由与 schema 零改动。
- 修复 Knowledge（`/knowledge/*`）与 Memory（`/memory/*`）未登录引导页在 Dark Mode 下仍以亮色渲染、与 Home（`/`）未登录引导视觉割裂的 UI 一致性缺陷：根因为 `apps/negentropy-ui/components/providers/AuthGuard.tsx` 的 `loading` 与 `unauthenticated` 分支硬编码 `bg-zinc-50`、`text-zinc-900`、`text-zinc-500`、`bg-black text-white` 等仅亮色 Tailwind 类，未提供任何 `dark:` 变体；而 Home 页 `app/page.tsx` L34–65 承载的同款引导早已补齐 `dark:bg-zinc-950` / `dark:text-zinc-400` / `dark:text-zinc-100` / `dark:bg-white dark:text-black`，两处 UI 形成事实上的双源漂移——Tailwind v4 `@variant dark (&:where(.dark, .dark *))` + `next-themes` `attribute="class"` 链路本身正常（`app/layout.tsx` L33–49、`app/globals.css` L2 / L73–93），仅 `AuthGuard` 硬编码类「截流」了 `.dark` cascade。以最小干预在 `AuthGuard.tsx` 两个分支补齐 6 处 `dark:` 变体（loading/unauth 容器、eyebrow、主标题、副文案、按钮）并为登录按钮新增 `dark:hover:bg-zinc-200`，语义与 Home 页逐字对齐；`app/knowledge/layout.tsx` / `app/memory/layout.tsx` 调用点零改动即生效。Home 页未登录引导与 `AuthGuard` 未登录引导仍存在完整 UI 重复，后续可抽取 `<LoginPrompt />` 共享组件收敛单一事实源（AGENTS.md「Reuse-Driven / SSOT」），本次不扩大爆炸半径。
- 修复 Home 页对话框发送消息返回 500（`ValueError: Agent not found: 'negentropy'`）：根因为 `cli.py` 的 `--reload_agents` 参数值 `src/negentropy` 使 ADK `AgentLoader` 在 `src/negentropy/` 下查找名为 `negentropy` 的子模块（期望 `src/negentropy/negentropy/`），但 Agent 实际定义在 `src/negentropy/` 本身（通过 `__init__.py` 的 `__getattr__` 导出 `root_agent`）。将 `agents_dir` 从 `src/negentropy` 改为 `src`，使 ADK 在 `src/` 下正确发现 `negentropy` 包；同步在 `src/` 下新建 `services.py` 作为 ADK `load_services_module` 的 service bridge，确保 `apply_adk_patches()`（日志、中间件、路由、模型缓存等）仍被正确加载。前端模型选择"自动变回 Default"为本次 500 错误的次生问题（后端未返回 STATE_DELTA 事件确认选择），后端修复后自动解决。
- 修复 Admin → Models 页 OpenAI Setting 模态框「Add Model」Save 时 `POST /api/auth/admin/model-configs` 返回 404：根因为 Next.js BFF 层缺少 `model-configs` 代理路由文件，补齐 `app/api/auth/admin/model-configs/route.ts`（GET/POST）与 `app/api/auth/admin/model-configs/[config_id]/route.ts`（PATCH/DELETE），与已有 `vendor-configs` 路由模式对齐。
- 修复 Admin → Models → Vendor（OpenAI）Edit 弹窗在自建网关（如 `http://llms.as-in.io`，同一网关下 Anthropic 与 Gemini Ping 均成功）下点击 **Ping** 返回 `litellm.RateLimitError: OpenAIException - Error code: 429 - {'meta': {'code': 429, 'source': 'gateway', 'type': 'TooManyRequests', 'message': 'API rate limit exceeded'}}`：根因为 litellm `1.83.x` 对 `openai/*` 将用户 `api_base` 原样作为 `AsyncOpenAI(base_url=...)`（见 `site-packages/litellm/llms/openai/openai.py::acompletion`），而 OpenAI Python SDK 以相对路径 `chat/completions` 拼接最终 URL（见 `site-packages/openai/resources/chat/completions/completions.py::create`），与 litellm 内置默认 `https://api.openai.com/v1` 的 `/v1` 版本段不对齐；用户按 OpenAI 官网 placeholder 风格填入裸 host `http://llms.as-in.io` 时请求落到 `http://llms.as-in.io/chat/completions`，网关因路径未注册以 catchall `429 TooManyRequests` 响应，反向伪装成「供应商限流」（对照 Anthropic 在 litellm 侧显式拼 `/v1/messages`、Gemini 已由上一条修复补齐 `/v1beta` 而均能成功）。扩展 `negentropy/config/model_resolver.py::normalize_api_base_for_litellm()` 新增 `openai/` 分支作为单一事实源：官方域名（`https://api.openai.com[/v1]`）→ `None` 放行 litellm 内置 URL；自建代理末尾无 `/v1` → 补齐 `/v1` 抵消 SDK 拼接偏差；用户误粘 `/chat/completions`、`/completions`、`/embeddings`、`/responses` 及其 `/v1/*` 变体时迭代剥离后再补齐；URL 中段已显式编码 `/v1`（如 `https://gateway/openai/v1`、`https://gateway/v1/custom`）恒等透传以兼容自定义多租户代理。`_ping_llm` / `_build_llm_kwargs` / `_build_embedding_kwargs` 复用同一规则覆盖 chat 与 embedding 全链路。
- 修复 Knowledge → Corpus 新建/编辑对话框及 Corpus 详情页 `ChunkingStrategyPanel` 中「Separators (one per line)」文本域键入单个 `\` 被自动扩写为 `\\`、无法删除或继续编辑成 `\n\n` 的 UX 阻塞：根因为 4 处 textarea 采用「受控组件 + 非幂等显示变换」反模式，将 `encodeSeparatorsForDisplay(decodeSeparatorsFromInput(input))` 直接作为 value 形成 round-trip，而该组合对「孤立反斜杠」这类中间态非幂等（decode 按字面量 1 字节保留，encode 再补写为 2 字节）。新增 `features/knowledge/components/SeparatorsTextarea.tsx` 将「原始输入字符串」抽离为本地显示缓冲，仅在外部 `value: string[]` 的**语义**变化（经 `separatorsArrayEqual` 判定，而非数组引用）时才重同步，避开 round-trip 抖动；4 处调用点（`CorpusFormDialog` recursive/hierarchical、`app/knowledge/base/page.tsx` recursive/hierarchical）统一替换；补齐 5 个 RTL 回归用例覆盖孤立 `\`、字面量 `\n`、外部重同步、等值引用稳定、退格清空五类行为。后端 schema 与 encode/decode 契约零改动。
- 修复 Admin → Models → Vendor（Gemini）Edit 弹窗点击 **Ping** 返回 `litellm.APIConnectionError: GeminiException - Received=<!DOCTYPE html>...window.__FEATURE_FLAGS__={...}`：根因为 litellm `1.83.x` 在 `vertex_ai/vertex_llm_base.py::_check_custom_proxy` 中一旦 `api_base` 非空即以 `{api_base}/models/{model}:{endpoint}` 拼接，丢失 Google 要求的 `/v1beta/` 版本段；用户按 placeholder 填入 `https://generativelanguage.googleapis.com` 会被路由到不存在的路径并被边缘以 HTML 兜底页响应，litellm 按 JSON 解析失败后抛 `GeminiException`。新增 `negentropy/config/model_resolver.py::normalize_api_base_for_litellm()` 作为单一事实源对 Gemini `api_base` 做正交归一（Google 官方域名 → `None` 放行 litellm 内置 URL；自建代理 → 补齐 `/v1beta` 抵消拼接偏差；非 `gemini/` 模型恒等透传），并在 `_ping_llm` / `_build_llm_kwargs` / `_build_embedding_kwargs` 三处共享同一规则，同步为 Ping 注入 `drop_params=True` 与 `_DEFAULT_LLM_KWARGS` 对齐。
- 修复 `adk web` 启动时 `MemoryAutomationFunctionResponse` 触发的 Pydantic `UserWarning: Field name "schema" in "MemoryAutomationFunctionResponse" shadows an attribute in parent "BaseModel"`：将 Python 属性名改为 `schema_name`，通过 `Field(alias="schema", serialization_alias="schema")` + `model_config = ConfigDict(populate_by_name=True)` 保持线协议（wire format）键名 `"schema"` 不变，前端 TS 类型与后端 SQL 数据零改动。
- 补齐 `opentelemetry-instrumentation-google-genai>=0.6b0,<1.0.0` 依赖，消除 ADK `adk_web_server` 启动期 `Unable to import GoogleGenAiSdkInstrumentor` WARNING，恢复 Google GenAI SDK 的 OTel 自动埋点，与现有 Langfuse OTel 链路打通。
- 站点级安静化两类先于 `negentropy.bootstrap` 触发的上游启动噪声（`AuthlibDeprecationWarning: authlib.jose module is deprecated` 与 `[EXPERIMENTAL] feature FeatureName.PLUGGABLE_AUTH is enabled`）：通过 `apps/negentropy/src/_negentropy_silence.pth`（hatchling `force-include` 至 site-packages 根）+ `negentropy/_silence_upstream_warnings.py` 在解释器 site 初始化期替换 `warnings.showwarning`，按消息子串白名单丢弃噪声；不影响任何其他告警通道，对 `authlib.deprecate` 的 `simplefilter("always", AuthlibDeprecationWarning)` 免疫（在 filter 之后的 showwarning 层拦截）。

### Removed

- 删除 `apps/negentropy/.env.example`（197 行）：其承载的全部非密钥配置项已在 `config.default.yaml` 中以结构化 YAML 形式表达，密钥类条目改为通过 shell 环境变量或 `.env.local` 提供。
- 下线 ZAI（Zhipu AI GLM）专属的 LiteLLM 集成链路：
  - 删除 `LlmVendor.ZAI` 枚举项；
  - 删除 `apps/negentropy/src/negentropy/config/pricing/glm_pricing.json` 与 `config/pricing/loader.py`、`config/pricing/models.py`（本地定价覆盖链路已由 LiteLLM 在线价目表统一收敛）；
  - 删除 `engine/bootstrap.py` 中 `NE_API_KEY → ZAI_API_KEY` 的环境变量映射；
  - 删除 `config/model_resolver.py` 中针对 ZAI vendor 的 thinking 处理分支；
  - `model_names.canonicalize_model_name()` 下线 GLM→zai 特化规则，退化为通用幂等 no-op（保留函数签名以维持上游调用正交性）。
- 彻底删除 `apps/negentropy-ui/lib/server/backend-url.ts` 的「历史端口迁移守护」（`LEGACY_LOCAL_PORTS` / `applyLegacyPortMigration` / `isLegacyLocalhostUrl` / `__resetLegacyPortWarningsForTests`）及其在 `.env.example`、`docs/development.md`、`tests/unit/lib/server/backend-url.test.ts` 中的关联配对：迁移守护本意为兼容 `:6600` / `:6666` → `:3292` 过渡期，但已完成迁移后继续残留反而构成熵源（让历史端口持续在代码、文档、测试夹具中循环出现，并与运维侧 `.env` 残留互相掩盖，导致「以为 PR 引入端口回退」的误判）。`3292` 为唯一权威端口；若本地 `.env` / `.env.local` 或 Google Cloud Console OAuth 授权重定向 URI 白名单仍残留 `:6600` / `:6666`，请一次性更新至 `:3292`（运维提示已同步进 `docs/sso.md`）。

### Changed

- Admin / Models → Interface / Models 信息架构迁移，同时彻底清除 `plugins` 命名残留，校正 Interface 二级导航顺序，并让 Dashboard 与 Nav 顺序严格对齐：
  - **IA 迁移**：Models 页（供应商凭证 + 模型注册 + Ping）归属从 Admin 迁到 Interface，二级路径 `/admin/models` → `/interface/models`；Admin 模块只保留 Users / Roles 治理职责，`AdminNav` 同步移除 Models 条目。
  - **命名统一**：UI 路径 `/plugins/*` → `/interface/*`、前端源码目录 `app/plugins/` → `app/interface/`、API 代理 `app/api/plugins/` → `app/api/interface/`、后端模块 `negentropy/plugins/` → `negentropy/interface/`（路由前缀 `/plugins` → `/interface`）、组件 `PluginsNav` → `InterfaceNav`、类型 `types/admin-models.ts` → `types/interface-models.ts`、组件 `components/admin/VendorModelsDisclosure.tsx` → `components/interface/VendorModelsDisclosure.tsx`；所有 `PluginStatsResponse` / `PluginsPage` / `PluginsLayout` 等符号同步更名为 `InterfaceStatsResponse` / `InterfacePage` / `InterfaceLayout`，不再残留 legacy `plugins` 概念。
  - **端点迁移**：原 `auth/api.py` 承载的 6 条 Models 路由（`/auth/admin/{vendor-configs,model-configs,models/ping}`）整体摘出至 `negentropy/interface/models_api.py`，前缀统一为 `/interface/models/*`；前端代理同步迁入 `app/api/interface/models/{vendor-configs,configs,ping}/`，`auth/api.py` 侧旧端点连同辅助函数一并移除。
  - **导航与 Dashboard 对齐**：`InterfaceNav` NAV_ITEMS 顺序改为 `Dashboard → Models → SubAgents → MCP → Skills`（SubAgents 上移至 MCP 之前）；`/interface` Dashboard 新增 Models StatCard（`total` / `enabled` / `vendors`）与「Manage Models」Quick Link，卡片 / Quick Link 顺序严格对齐 Nav。
  - **权限双层守卫（保守策略）**：Models 权限父项向 `interface:*` 命名看齐（`rbac.PERMISSIONS` 新增 `interface:read` / `interface:write`，`user` 角色获得 `interface:read`/`interface:write`，`admin` 角色以 `interface:*` 通配覆盖），但后端仍保留 `"admin" in current_user.roles` 的 role 校验（403），并在前端 `InterfaceNav` / Dashboard Models 入口基于 `useAuth().user.roles` 条件渲染，`/interface/models/page.tsx` 内部以 `useEffect` 重定向守卫兜底，避免非 admin 绕过入口直接访问。
  - **后端 `/interface/stats` 扩展**：聚合响应新增 `models: { total, enabled, vendors }` 字段，`total` 来自 `ModelConfigRecord` 总数、`enabled` 按 `ModelConfigRecord.enabled=True` 计数、`vendors` 为启用 `VendorConfig` 的数量。
  - **测试同步**：`tests/unit/plugins/` → `tests/unit/interface/` 整目录迁入；`PluginsNav.test.tsx` → `InterfaceNav.test.tsx` 并新增 Models 入口 admin/非 admin 可见性、NAV_ITEMS 顺序断言；`InterfacePage.test.tsx` 新增 Models StatCard 与 Quick Link 的条件渲染断言；后端 `tests/unit_tests/interface/test_models_api.py` / `test_models_ping.py` 从 auth 目录迁入并把 URL 断言改为 `/interface/models/*`，`test_deps_and_rbac.py` 同步更新 `interface:write` 断言。
- 默认 LLM 模型由 `zai/glm-5` 切换为 `openai/gpt-5-mini`；模型 vendor（OpenAI / Anthropic / Gemini 等）通过 Interface → Models 页动态配置，数据源为 `model_configs` 表。
- 激进重构 `apps/negentropy/src/negentropy/config/config.default.yaml`：
  - 引入 `_constants` YAML anchor/alias 块，消除 ≥7 处魔法数字/字符串重复；
  - 顶级 `env:` 重组为 `environment.env`，与其他 8 个子块结构对齐（对 legacy 顶级 `env:` 保留向后兼容回退）；
  - `database.url` 去凭证化为模板值 `postgresql+asyncpg://USER:PASSWORD@localhost:5432/negentropy`；
  - `auth.cookie_secure` 注释增加「生产必须为 true」警示；
  - 头部注释增补密钥类环境变量清单（OpenAI / Anthropic / Gemini / Langfuse / Google Search 等）。
- 为 9 个 Settings 类（主 `Settings` + 8 子 Settings）启用 Pydantic `env_nested_delimiter="__"`，支持扁平环境变量覆盖深层嵌套字段，例如 `NE_KNOWLEDGE_DEFAULT_EXTRACTOR_ROUTES__URL__PRIMARY__TIMEOUT_MS=90000`。
- 文档统一推荐 `uv run negentropy init` 生成 `~/.negentropy/config.yaml`，密钥通过 shell 环境变量或 `.env.local` 注入。

### Breaking

- 合并 `0001_init_schema.py` / `0002_add_vendor_configs.py` 为单一 `0001_init_schema.py`（revision id 重置为 `0001`）；升级路径：**需先 `DROP SCHEMA negentropy CASCADE` 再 `uv run alembic upgrade head`**（项目处于早期阶段，无线上负担，不做自动兼容）。
- 移除 6 个孤岛型 ORM 与对应表：`SandboxExecution`、`Instruction`、`ConsolidationJob`、`Run`、`Message`、`Snapshot`；两轮审计下均为业务零引用，遵循熵减原则。配套裁撤 `Thread.{runs,messages,snapshots,consolidation_jobs}` 与 `Event.messages` 五个 relationship。业务表数由 42 降为 37。
- 新迁移不包含任何种子数据（`model_configs` / `mcp_servers` 预设）；默认配置改由应用侧初始化流程（如 `negentropy init` 或管理员首次登录）承担。
- 用户级 `~/.negentropy/config.yaml` 或部署脚本中若配置了 `zai/*` 模型，需在 Admin → Model 页迁移至 OpenAI / Anthropic / Gemini 等 vendor 重新配置；历史 DB 中 `vendor=zai` 的 `model_configs` 记录不会被自动清理，但调用时将因缺失解析逻辑而返回「模型未配置」。
- 下线 `NE_API_KEY → ZAI_API_KEY` 映射；依赖该映射的部署需改为直接设置各 vendor 原生环境变量（如 `OPENAI_API_KEY`、`ANTHROPIC_API_KEY`）。
